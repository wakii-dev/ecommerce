<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>com.ecommerce</groupId>
  <artifactId>ecommerce-backend</artifactId>
  <version>0.1.0-SNAPSHOT</version>
  <packaging>pom</packaging>
  <name>ecommerce-backend</name>
  <description>Ecommerce platform — backend parent (Spring Boot 3 / Java 21)</description>

  <!--
    Pre-pin conventions (R3 — SF sau ít đụng pom chung):
    - Version đặt ở <properties> dưới đây; module KHÔNG tự đặt version.
    - BOM: spring-boot-dependencies + spring-cloud-dependencies + testcontainers-bom.
    - Module mới append <module> ở dưới + kế thừa dependencyManagement, không import BOM riêng.
  -->

  <properties>
    <java.version>21</java.version>
    <maven.compiler.release>21</maven.compiler.release>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>

    <spring-boot.version>3.3.5</spring-boot.version>
    <spring-cloud.version>2023.0.3</spring-cloud.version>
    <springdoc.version>2.6.0</springdoc.version>
    <testcontainers.version>1.21.3</testcontainers.version>
    <stripe-java.version>24.16.0</stripe-java.version>

    <maven-compiler-plugin.version>3.13.0</maven-compiler-plugin.version>
    <maven-surefire-plugin.version>3.2.5</maven-surefire-plugin.version>
  </properties>

  <!-- Modules (append-only per SF — thêm cùng commit tạo module):
       shared/common-lib · gateway · services/template-service · services/<svc>-service (SF-3+) -->
  <modules>
    <module>shared/common-lib</module>
    <module>gateway</module>
    <module>services/template-service</module>
    <!-- SF-3 (FI-313) -->
    <module>services/identity-service</module>
    <!-- SF-4 (FI-314) -->
    <module>services/catalog-service</module>
    <!-- SF-5 (FI-315) -->
    <module>services/inventory-service</module>
    <module>services/payment-service</module>
    <!-- SF-6 (FI-316) -->
    <module>services/cart-service</module>
    <!-- SF-9 (FI-319) -->
    <module>services/ordering-service</module>
    <!-- SF-11 (FI-321) -->
    <module>services/partner-api</module>
  </modules>

  <dependencyManagement>
    <dependencies>
      <dependency>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-dependencies</artifactId>
        <version>${spring-boot.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
      <dependency>
        <groupId>org.springframework.cloud</groupId>
        <artifactId>spring-cloud-dependencies</artifactId>
        <version>${spring-cloud.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
      <dependency>
        <groupId>org.testcontainers</groupId>
        <artifactId>testcontainers-bom</artifactId>
        <version>${testcontainers.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>

      <!-- API docs (springdoc — tương thích Boot 3.3) -->
      <dependency>
        <groupId>org.springdoc</groupId>
        <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
        <version>${springdoc.version}</version>
      </dependency>

      <!-- Payment (SF-5) -->
      <dependency>
        <groupId>com.stripe</groupId>
        <artifactId>stripe-java</artifactId>
        <version>${stripe-java.version}</version>
      </dependency>

      <!-- common-lib nội bộ — mọi service dùng bản reactor -->
      <dependency>
        <groupId>com.ecommerce</groupId>
        <artifactId>common-lib</artifactId>
        <version>${project.version}</version>
      </dependency>
    </dependencies>
  </dependencyManagement>

  <build>
    <pluginManagement>
      <plugins>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-compiler-plugin</artifactId>
          <version>${maven-compiler-plugin.version}</version>
          <configuration>
            <release>${maven.compiler.release}</release>
            <parameters>true</parameters>
          </configuration>
        </plugin>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-surefire-plugin</artifactId>
          <version>${maven-surefire-plugin.version}</version>
        </plugin>
        <plugin>
          <groupId>org.springframework.boot</groupId>
          <artifactId>spring-boot-maven-plugin</artifactId>
          <version>${spring-boot.version}</version>
        </plugin>
      </plugins>
    </pluginManagement>
  </build>
</project>
