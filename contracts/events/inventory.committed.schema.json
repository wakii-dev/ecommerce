{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "inventory.committed.schema.json",
  "title": "inventory.committed",
  "description": "Trừ tồn chính thức khi order PAID (commit reservation). Producer: inventory-service. Kích hoạt qua event order.paid — không qua REST.",
  "type": "object",
  "required": ["eventId", "eventType", "occurredAt", "correlationId", "producer", "schemaVersion", "payload"],
  "properties": {
    "eventId": {
      "type": "string",
      "format": "uuid",
      "description": "UUID v4 — consumer dùng dedupe (idempotent receive)"
    },
    "eventType": {
      "type": "string",
      "pattern": "^[a-z]+\\.[a-z-]+$",
      "const": "inventory.committed",
      "description": "Routing key dạng <domain>.<event>"
    },
    "occurredAt": {
      "type": "string",
      "format": "date-time",
      "description": "Thời điểm sự kiện xảy ra (UTC, ISO 8601)"
    },
    "correlationId": {
      "type": "string",
      "description": "Truyền từ header X-Request-Id của gateway"
    },
    "producer": {
      "type": "string",
      "description": "Tên service phát event"
    },
    "schemaVersion": {
      "type": "integer",
      "minimum": 1,
      "default": 1,
      "description": "Phiên bản payload schema — additive-only"
    },
    "payload": {
      "type": "object",
      "required": ["reservationId", "orderId", "items"],
      "properties": {
        "reservationId": {
          "type": "string",
          "description": "Id reservation đã commit"
        },
        "orderId": {
          "type": "string"
        },
        "items": {
          "type": "array",
          "minItems": 1,
          "items": {
            "type": "object",
            "required": ["variantId", "qty"],
            "properties": {
              "variantId": {
                "type": "string",
                "description": "Reservation ở mức variant (không phải product)"
              },
              "qty": {
                "type": "integer",
                "minimum": 1
              }
            }
          }
        }
      }
    }
  }
}
