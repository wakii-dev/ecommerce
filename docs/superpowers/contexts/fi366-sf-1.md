# FI-366 SF-1 Context Pack — backend-e2e-stabilize

> Đọc file này THAY VỀ tự tổng hợp. Epic spec: `docs/superpowers/specs/2026-09-07-qa-polish-v11-design.md` · Bracket: `docs/superpowers/brackets/fi366-qa-polish-v11.md` · Linear: FI-366 (epic) · Nhánh đích: `story/fi366-qa-polish-v11` (fork từ master GA)
> `contracts/**` READ-ONLY — amendment A2 (admin coupons) do coordinator apply TRƯỚC khi bạn start; nếu chưa thấy → REQUIREMENT-GAP lên FI-366.

## Spec slice (chỉ phần SF-1 chịu trách nhiệm)

1. **Suite runner chuẩn hóa** `scripts/test/`: `run-java.sh` (mvn -q test reactor từ `backend/`, ghi chú `-am` + repackage-trap từ improvements-log) · `run-fe.sh` (pnpm -r test qua turbo) · `run-pytest.sh` (invoice .venv) · `run-e2e.sh` (serial).
2. **Run-batch toàn bộ** → **bug register** `scripts/test/bug-register.md` (living doc, P0/P1/P2 + loại env-infra/product-bug/flaky; flaky retry 1 lần; P1>15 → báo coordinator tách story). Java ×11 module + FE 36 file + pytest invoice.
3. **Preflight**: `make dev` fullstack 1 session (SF-1 own start/stop) · `make seed` ×2 idempotency · record GATEWAY_URL/port · **dev-stack token TTL re-mint + single-session guard** (dev-stack.sh hiện KHÔNG có guard — thêm pid-check).
4. **E2E không-keys**: inventory chính xác các test skip `[PENDING-STRIPE-KEYS]` (grep `hasStripe` trong `frontend/e2e/` — hit: playwright.config, golden-path, saga-fail, platform-asserts, helpers/env — KHÔNG có review-flow, đừng hard-code).
5-8. **Browser walkthrough** so direction `docs/superpowers/designs/fi310-storefront-direction.md` §1-3: storefront (home/PLP/PDP/cart) + shell/checkout/account + admin (§2.6 pill/KPI/table) → diff list vào register.
9. **Theme audit**: tokens.css 3 theme (storefront/admin/dark) — completeness, hex rời, contrast dark (WCAG AA text).
10. **A11y + i18n + secrets sweep**: focus-visible, i18n key missing (đối chiếu FI-317 improvements-log: `admin.common.from/to`, OrderDetailPage hardcoded), hardcoded string scan 5 apps, secrets grep (đã có `.gitignore` infra/keys ×2 — dọn duplicate).
11. **FI-337 residual** (delta còn lại — backend/AuthStore/AdminApp ĐÃ fix từ GA, KHÔNG đụng): repro Set-Cookie xuyên `shell/vite.config.ts` + `mfe-account/vite.config.ts` → fix + regression test + cookie e2e assert.
12. **RBAC fixes**: `gateway-routes.yml` admin-prefixes THÊM `/api/inventory/admin/**` + `/api/catalog/admin/**` + inventory-service Spring Security guard tối giản (pom thiếu dep security — thêm oauth2-resource-server + JwtDecoder JWKS identity).
13. **AdminCouponController** (ordering): POST/PUT/DELETE `/admin/coupons` theo contracts amendment A2 (coordinator apply — kiểm `git log contracts/` trước) + validation + IT.
14. **GATEWAY_URL single-source — 13 file thật** (grep-verified): 4 vite configs (mfe-account/shell/mfe-checkout/mfe-admin) · `storefront-web/lib/catalog-api.ts` · `lib/reviews-api.ts` · `middleware.ts` · `app/[locale]/coupons/page.tsx` · `tests/wiring.test.ts` · `scripts/render-smoke.mjs` · `e2e/helpers/env.ts` · `scripts/dev-stack.sh` · `next.config.mjs`. Cơ chế: `.env` nguồn duy nhất, FE `import.meta.env`, Next `process.env`; fix = thay hardcode `:8080` bằng env + documented default. Sau fix: `make full` sanity re-run.
15. **Seed/coupon repair**: verify checksum V11 volume dev trước → **V12 migration** (WELCOME10/GIAM50K limit/expiry theo pack sf-10) hoặc seed.sh UPSERT-only; stock repair `DO UPDATE` giữ idempotent — chạy đúng cả volume cũ + compose fresh.
16. **Stripe-enabled re-run** (khi keys có mặt `.env` — user hứa cung cấp): restart stack nhạt `.env` → chạy các test skip per T4 inventory; FAIL stripe-branch → fix + chạy lại. Không keys khi hết story → PENDING documented.
17. Security re-sweep + update `docs/superpowers/improvements-log.md` (đánh dấu mục đã xử lý).

## Touch map (files SF-1 tạo/sở hữu)

```
scripts/test/** (mới) · scripts/test/bug-register.md
scripts/dev-stack.sh (TTL + guard) · scripts/seed/seed.sh (stock/coupon UPSERT)
backend/services/ordering-service/src/main/resources/db/migration/V12__*.sql
backend/services/inventory-service/** (security guard + pom dep)
backend/gateway/src/main/resources/gateway-routes.yml (append 2 admin prefixes)
backend/services/ordering-service/**/AdminCouponController + IT
backend/services/identity-service/src/main/resources/... (CHỈ nếu FI-337 fix đụng — theo spec KHÔNG cần)
frontend/apps/shell/vite.config.ts · frontend/apps/mfe-account/vite.config.ts (Set-Cookie)
frontend/apps/*/vite.config.ts + storefront-web 8 file + e2e/helpers/env.ts (GATEWAY_URL)
.env.example (append GATEWAY_URL documented default)
docs/superpowers/improvements-log.md (đánh dấu mục xử lý)
```
READ-ONLY: `contracts/**`, `frontend/packages/**` (trừ bug blocks SF-1), pages/components của SF-2 (storefront-web: chỉ 8 file GATEWAY_URL + middleware thuộc SF-1), `AdminApp.tsx` (SF-2).

## ACCEPTANCE (user-visible)

1. `bash scripts/test/run-java.sh` → full Java suite XANH (bao gồm cả test từng bị nuốt `*IT`).
2. `run-fe.sh` + `run-pytest.sh` XANH.
3. `make dev` 1 session sạch; seed ×2 idempotent; GATEWAY_URL 1 nguồn.
4. E2E không-keys: toàn bộ test không-skip XANH; skip list inventory chính xác.
5. Customer JWT gọi `/api/inventory/admin/low-stock` → **403**; `/api/catalog/admin/**` qua gateway → 403 trước 404.
6. Admin coupon CRUD qua API mới: tạo/sửa/xóa → coupon center hiện đúng.
7. FI-337: cookie refresh Set-Cookie xuyên vite proxy chuẩn (e2e assert).
8. Bug register P0/P1 = 0 open sau fix batch.

## Boundary (KHÔNG làm)

- KHÔNG đụng `frontend/apps/mfe-admin/src/AdminApp.tsx` + pages/theme (SF-2) — trừ 2 vite configs Set-Cookie.
- KHÔNG sửa `contracts/**` (amendment A2 do coordinator). KHÔNG thêm dep FE.
- KHÔNG sửa V11 đã apply (V12 mới). KHÔNG refactor auth flow (chỉ fix race cụ thể).
- KHÔNG chạy `make full` khi disk < 20G (kiểm `df` trước — đã ENOSPC 2 lần).
