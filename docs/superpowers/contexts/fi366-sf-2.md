# FI-366 SF-2 Context Pack — frontend-polish-theme

> Đọc file này THAY VỀ tự tổng hợp. Epic spec: `docs/superpowers/specs/2026-09-07-qa-polish-v11-design.md` · Bracket: `docs/superpowers/brackets/fi366-qa-polish-v11.md` · Linear: FI-366 (epic) · Nhánh đích: `story/fi366-qa-polish-v11` (fork từ master GA)
> `contracts/**` READ-ONLY. **KHÔNG chạy `make dev` fullstack — SF-1 own lifecycle; bạn là CONSUMER** (E4): `make dev-fe app=<name>` + GATEWAY_URL trỏ stack SF-1 đang chạy.

## Spec slice (chỉ phần SF-2 chịu trách nhiệm)

1. **Tokens diff vs direction §1** (`fi310-storefront-direction.md`): hex/spacing/radius/shadow/typography — chỉ fix VALUE trong `tokens.css`, không đổi tên biến. Đã có `[data-theme='storefront'|'admin'|'dark']`.
2. **Dark mode polish**: hoàn thiện `[data-theme='dark']` theo contrast WCAG AA text; không hex rời ngoài tokens.
3. **Storefront header + home so §2.1-2.2**: sticky header, search bar trung tâm, mini-nav, hero carousel, flash-deal countdown, featured grid — fix diff-vs-spec (không redesign).
4. **Product-card + PLP + PDP so §2.3-2.5**: badge tint §1.6, price-block wash #FFF1F0, variants swatch/chip, tabs, breadcrumb, reviews layout.
5. **`AdminApp.tsx` trọn vẹn thuộc SF-2** (AdminShell sidebar `admin-side` + topbar + layout :79-138 + theme swap :173-180): polish shell/KPI/table theo §2.6 + pill palette §1.7. KHÔNG đụng logic auth boot (đã fix từ GA —FI-337).
6. **Behavior §3**: hover-lift transition .12s, focus-visible outline, button variants mapping, giá VND format qua `Price` primitive nhất quán 5 apps.
7. **i18n fixes**: `admin.common.from/to` thiếu cả vi/en (improvements-log FI-317) · `OrderDetailPage` hardcoded tiếng Việt → `useT` · scan theo bug register SF-1 (coordinator relay) · hardcoded string scan 5 apps (SF-1 T-audit cung cấp list).
8. **UX guards**: confirm modal delete categories/coupons · `lib/productForm.ts` numeric guard ("1e999" → Infinity) · empty/error states theo ngôn ngữ tint §4.
9. **Responsive ≤900px**: header wrap, mini-nav scroll-x, admin sidebar collapse.
10. **FE unit tests**: fix test vỡ do polish (theme.test, uiKit.test, storefront tests/*) + thêm token-regression test nhỏ.
11. **Browser walkthrough sau-fix**: day-1 rig preflight (chứng minh 1 trang round-trip FE-local `make dev-fe app=<name>` + gateway-remote TRƯỚC khi polish — cross-worktree MF host/remote chưa từng chứng minh) + screenshot record 5 màn. KHÔNG chạy E2E (SF-1 own).
12. *(artifact, không phải task)* Hand-off notes cho SF-1 T15 (stripe re-run).
13. **Dep freeze**: cấm thêm dep FE mới nếu không note coordinator; nếu có → re-run `pnpm install` lúc merge (pnpm-lock risk ≈ 0 — turbo test có sẵn).

## Touch map (files SF-2 tạo/sở hữu)

```
frontend/packages/ui-kit/src/styles/tokens.css (3-4 theme + dark contrast)
frontend/apps/mfe-admin/src/** (pages/theme/i18n/guards — TRỪ file SF khác nếu có)
frontend/apps/storefront-web/src/** (theme/section polish — theo diff list SF-1)
frontend/apps/shell + mfe-account + mfe-checkout: theme polish + i18n keys
frontend/packages/i18n/src/** (keys thiếu từ SF-1 audit)
docs/superpowers/designs/fi310-storefront-direction.md = CHUẨN so sánh
```
READ-ONLY: `contracts/**`, backend/**, `packages/{auth,contracts,config}`, e2e/ (SF-1 own).

## Dep states

- SF-1 backend-e2e-stabilize chạy SONG SONG — **KHÔNG phụ thuộc**: bạn chạy unit + browser static + `make dev-fe` đơn app. Nếu cần stack đầy đủ để walkthrough: dùng dev-stack do SF-1 đang giữ (GATEWAY_URL trỏ đúng) — phối hợp qua task message, KHÔNG tự start/stop.
- Bug register SF-1: coordinator relay deltas — kiểm tra task message định kỳ.
- Design direction hand-off: `docs/superpowers/designs/fi310-storefront-direction.md` §1-3 (đã chọn hướng A "Chợ Sôi Động").

## ACCEPTANCE (user-visible)

1. Walkthrough 5 màn (home/PLP/PDP/cart/checkout) pass vs direction §2.1-2.5 — screenshot record.
2. Admin shell/KPI/tables/pill đúng §2.6 + theme admin.
3. Dark mode: toggle → toàn bộ 5 apps đổi theme, reload giữ nguyên, contrast AA text pass.
4. i18n: `admin.common.from/to` có key vi/en; OrderDetailPage dùng useT (khó hardcoded string); hardcoded scan 5 apps = 0 P1.
5. UX guards: confirm delete categories/coupons; productForm chặn "1e999"; empty/error states đúng tint.
6. Responsive ≤900px: header wrap + mini-nav scroll-x + admin sidebar collapse không vỡ layout.
7. FE unit tests 36 file XANH sau polish + token-regression test mới.

## Boundary (KHÔNG làm)

- KHÔNG fullstack (E4) — KHÔNG chạy `make dev`/`make full` (SF-1 own).
- KHÔNG chạy E2E Playwright (SF-1 own). KHÔNG sửa backend.
- KHÔNG thêm feature mới — CHỈ polish theo checklist trên.
- KHÔNG đụng `packages/{auth,contracts}` logic — CHỈ tokens.css nếu cần dark variant.
- pnpm-lock: cấm thêm dep không note coordinator.
