# FI-369 SF-1 Context Pack — stripe-live-e2e

> Đọc file này THAY VỀ tự tổng hợp. Epic spec: `docs/superpowers/specs/2026-09-07-nofallback-feature-complete-design.md` · Bracket: `docs/superpowers/brackets/fi369-nofallback-complete.md` · Linear: FI-366 (epic) · Nhánh đích: `story/fi369-nofallback-complete` (fork từ master GA)

## Spec slice (chỉ phần SF-1 chịu trách nhiệm)

1. **Probe trước**: đọc `PaymentWebhookService` (luồng PAID webhook-only hay confirm-sync) + `stripe --version` probe máy (chưa cài → thêm setup step).
2. **Tạo Stripe account qua Orca browser** (hybrid N3 — automation ~70%): signup `dashboard.stripe.com/register` — email `toilahoi007@gmail.com` + password mạnh + country (Singapore khuyến nghị — VN chưa được Stripe hỗ trợ đầy đủ) → **Turnstile đã tự giải trước đó** (token input được điền) → verify email (**ESCAPE HATCH: link verify trong Gmail `toilahoi007@gmail.com` — USER click**) → skip business form (test mode) → Dashboard → Developers → API keys → copy `pk_test_`/`sk_test_`.
3. **Tạo `.env` repo-root** (gitignored — verify trước) + điền ĐÚNG 3 biến hard-require (per `e2e/helpers/env.ts:47-58`): `STRIPE_SECRET_KEY=sk_test_*`, `VITE_STRIPE_PUBLISHABLE_KEY=pk_test_*`, `STRIPE_WEBHOOK_SECRET=whsec_*`; probe consumer pk (checkout UI Elements hay chỉ e2e helper) ghi vào T5.
4. **Makefile `stripe-listen`**: reuse compose service stripe-cli có sẵn (`docker-compose.yml:139-150`, `STRIPE_API_KEY` env, forward → `:8086/payment/webhook`) + `stripe login` non-interactive qua `--api-key`; whsec CLI in ra điền `.env`; verify payment-service nhận webhook → đơn PAID.
5. **Un-skip + golden-path E2E với keys**: bước đầu **hard-assert `hasStripe() === true`** (extend `e2e/helpers/env.ts:47-58` đòi thêm `whsec_` prefix — hiện chỉ sk+pk) → 2 test `[PENDING-STRIPE-KEYS]` (golden-path :192,:201) PASS thật (4242 CONFIRMED + 4000...0002 declined).
6. Saga-fail declined + platform-asserts commission PASS thật.
7. Regression không-keys: `payUnavailable` 503 fail-loud giữ đúng.
8. Evidence: screenshot dashboard keys (che giữa) + E2E green log + `[PENDING-STRIPE-KEYS]` → `[VERIFIED-STRIPE]`.
9. Webhook local runbook vào README.
10. Gate slice: Java payment IT + E2E golden-path green.

## Touch map (files SF-1 tạo/sở hữu)

```
.env (TẠO MỚI — gitignored verify) · .env.example (comment update)
Makefile (target stripe-listen)
frontend/e2e/helpers/env.ts (extend hasStripe whsec_)
frontend/e2e/tests/golden-path.spec.ts (un-skip 2 test)
docs/README webhook runbook section
```
READ-ONLY: `contracts/**`, toàn bộ services code (payment KHÔNG đổi — wiring env only).

## ACCEPTANCE (user-visible)

1. Stripe account test tồn tại (email toilahoi007@gmail.com); keys trong `.env` KHÔNG commit (grep verify).
2. E2E golden-path: 2 test PENDING-STRIPE-KEYS PASS thật (4242 CONFIRMED + declined FAILED).
3. Saga-fail + platform-asserts + commission-ledger PASS thật.
4. Không-keys → payUnavailable 503 fail-loud giữ đúng (regression).
5. Webhook local: `stripe listen` → payment PAID qua webhook E2E.
6. Gate slice: Java payment IT + E2E golden-path green.

## Boundary (KHÔNG làm)

- KHÔNG đổi payment-service code (wiring env only — application.yml slots có sẵn).
- KHÔNG đụng contracts, SF-2/SF-3 files.
- Escape hatch N3: CAPTCHA/phone/dashboard drift → USER làm tay ≤10 phút (coordinator raise ngay) — KHÔNG treo.
- KHÔNG commit keys (.env gitignored — verify trước mỗi commit).
