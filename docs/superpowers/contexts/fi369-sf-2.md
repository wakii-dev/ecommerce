# FI-369 SF-2 Context Pack — coupon-crud-a3

> Đọc file này THAY VỀ tự tổng hợp. Epic spec: `docs/superpowers/specs/2026-09-07-nofallback-feature-complete-design.md` · Bracket: `docs/superpowers/brackets/fi369-nofallback-complete.md` · Linear: FI-366 (epic) · Nhánh đích: `story/fi366-qa-polish-v11`
> `contracts/**` READ-ONLY — amendment A3 do coordinator apply (kiểm `git log contracts/` trước khi code; chưa thấy → REQUIREMENT-GAP lên FI-366).

## Spec slice (chỉ phần SF-2 chịu trách nhiệm)

**BASELINE QUAN TRỌNG: `AdminCouponController` ĐÃ TỒN TẠI trên master** (FI-366 SF-1 T11, commit 89adcd9): create/update/DELETE `/admin/coupons`, `@PreAuthorize ADMIN`, `AdminCouponDto` 10 trường (usageLimit/usedCount/active), 409/400/404 mapping. SF-2 = **RECONCILE + BỔ SUNG**, KHÔNG build net-new.

1. **REQUIREMENT-GAP A3 proposal** xuất phát từ shape hiện có: endpoint THIẾU = `GET /admin/coupons` (list trả AdminCouponDto — public list chủ động KHÔNG lộ usedCount) + toggle `PUT /{code}/active`; DTO mở rộng nếu thiếu field — chờ coordinator duyệt + amend `contracts/openapi/ordering.yaml` + regen TS.
2. **Probe gateway allowlist** `/api/ordering/admin/**` (gateway-auth.yml:53 đã allow từ SF-9 — verify) + đọc `CouponReservationRepository` → chốt DELETE/deactivate policy.
3. **BE reconcile**: AdminCouponController bổ sung GET list + toggle endpoint + khớp DTO — KHÔNG đụng create/update/DELETE đã đúng.
4. **BE `CouponService` admin methods + deactivate policy (N4)**: reserve = guarded UPDATE check `active` + `usedCount++` LÚC RESERVE → **toggle/deactivate giữa chừng → đơn in-flight (RESERVED) tôn trọng, mã mới từ chối** — KHÔNG delete+recreate; DELETE cứng chỉ khi chưa reservation.
5. **BE IT** contract-shape + validation (bổ sung GET list + toggle-in-flight case).
6. **FE `CouponsPage`**: form create/edit + toggle + delete + error surfaces (409/422) — XOÁ note read-only (dòng 88-96).
7. **FE types regen** + unit test form validation.
8. **E2E admin coupon** (KHÔNG cần keys — assert sau RESERVED): tạo → checkout dùng → usedCount tăng → toggle off → validate fail.
9. **Seed parity**: `make seed` coupons hiển thị đúng qua list mới.
10. **Gate slice**: Java + FE + E2E admin-coupon green.

## Touch map (files SF-2 tạo/sở hữu)

```
backend/services/ordering-service/**/AdminCouponController (reconcile) · CouponService admin methods
backend/services/ordering-service/.../IT (bổ sung GET list + toggle)
frontend/apps/mfe-admin/src/pages/CouponsPage.tsx (form CRUD — XOÁ note read-only)
frontend/packages/contracts/src/generated (regen — sau coordinator amend)
```
READ-ONLY: `contracts/**` (A3 do coordinator), checkout UI, payment/inventory.

## ACCEPTANCE (user-visible)

1. Admin tạo coupon qua UI → coupon center hiện; sửa window/limit → validate đúng.
2. Toggle off → checkout validate fail; toggle on → dùng được lại.
3. DELETE coupon chưa reservation → xoá; đã reservation → deactivate (in-flight tôn trọng).
4. GET list trả AdminCouponDto đầy đủ (usageLimit/usedCount/active).
5. Gate slice: Java + FE + E2E admin-coupon green.

## Boundary (KHÔNG làm)

- KHÔNG đụng checkout UI logic (đã đúng). KHÔNG đụng payment/inventory.
- KHÔNG sửa `contracts/**` (A3 coordinator apply). KHÔNG feature mới ngoài CRUD.
- Stripe keys: thuộc SF-1 — KHÔNG đụng `.env` Stripe fields.
