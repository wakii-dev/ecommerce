# FI-369 SF-3 Context Pack — honesty-pass

> Đọc file này THAY VỀ tự tổng hợp. Epic spec: `docs/superpowers/specs/2026-09-07-nofallback-feature-complete-design.md` · Bracket: `docs/superpowers/brackets/fi369-nofallback-complete.md` · Linear: FI-366 (epic) · Nhánh đích: `story/fi366-qa-polish-v11`
> **Tier 1 — chạy SAU SF-2 merge + SF-1 keys**: T1-T9 có thể song song sớm nhưng T10 (grep sweep cuối) + T11 (integration gate) CHỈ sau khi cả SF-1/2 merge.

## Spec slice (chỉ phần SF-3 chịu trách nhiệm)

1. **Header mini-nav** (Header.tsx:77-79 ĐÃ trỏ `/c/dien-tu?sort=newest|rating`): **probe slug `dien-tu` tồn tại trong seed/DB trước** — sort values code thật `newest|rating`. Không thấy slug → decision: hardcode slug có thật hoặc bỏ link (nói với coordinator).
2. **CategoryTiles "more"** (CategoryTiles.tsx:51) + **home "Xem thêm"** (page.tsx:85 — hiện `href="#"`): → PLP/category route thật.
3. **Footer** (Footer.tsx:34): trim về routes có thật (N5 — không tạo trang content mới).
4. **`AddToCart.tsx:36,45`** toastFail "Cart is coming soon" → lỗi thật ("Không thêm được vào giỏ — thử lại"); verify: unit test error state + E2E route.abort assert toast.
5. **Xoá dead i18n `reviewsSoon`** (p/[slug]/page.tsx:51,69) — grep test tham chiếu trước.
6. **Tách `canShip/canDeliver/canCancel` + test** → `lib/orderRules.ts`; **`tests/stub.test.ts` SPLIT**: order-rules giữ thành `orderRules.test.ts`, mock-API test XOÁ; xoá `createStubApi`+seed khỏi `adminStub.ts`; xoá file rỗng.
7. **Rename `Stub*` types** (`lib/types.ts` + Dashboard/Orders/OrderDetail/Reviews pages — CouponsPage KHÔNG dùng Stub, dùng PublicCoupon; thêm `lib/api.ts` stub re-exports) — mechanical 1 commit.
8. **Shell route `/ui-kit` gate** env flag `NEXT_PUBLIC_UIKIT_DEV=1` (unset = ẩn prod).
9. **Degraded-by-design registry** (ADR): ES→PgFts GIỮ (D15) · payment 503 GIỮ · GHN flat GIỮ + nhãn UI "Phí tiêu chuẩn" — 3-question test N2 làm khung.
10. **Grep sweep cuối** — scope `storefront-web/src` + `mfe-admin/src`: pattern `href="#"`, `Sắp ra mắt`, `createStubApi` → zero; whitelist enumerate từng match sống kèm lý do (vi.mock, `placeholder=` attr, `_skeleton-remote`, ui-kit demo).
11. **Playwright nav asserts** (binary hoá): header/footer/tiles click → URL đích đúng, không `href="#"`.

## Touch map (files SF-3 tạo/sở hữu)

```
frontend/apps/storefront-web/src/components/{Header,Footer,CategoryTiles}.tsx (edit route/link)
frontend/apps/storefront-web/src/components/home/page.tsx (Xem thêm)
frontend/apps/storefront-web/src/components/pdp/AddToCart.tsx (toastFail)
frontend/apps/storefront-web/app/[locale]/p/[slug]/page.tsx (:51,69 i18n dead)
frontend/apps/mfe-admin/src/lib/{orderRules.ts mới, adminStub.ts xoá} · lib/types.ts (rename Stub*)
frontend/apps/mfe-admin/src/tests/stub.test.ts (split)
frontend/apps/shell/src/App.tsx (:173-175 /ui-kit env gate)
docs/superpowers/designs/ + ADR registry
```
READ-ONLY: `contracts/**`, backend services (KHÔNG đụng — sweep chỉ FE), packages/{auth,contracts}.

## ACCEPTANCE (user-visible)

1. Mọi link header/footer/tiles/"Xem thêm" điều hướng đến trang thật (không `href="#"`, không dead).
2. `AddToCart` toastFail = lỗi thật (không "coming soon").
3. `reviewsSoon` i18n dead keys xoá; grep sweep cuối = 0 match ngoài whitelist.
4. `Stub*` types = 0; `createStubApi` xoá; orderRules test xanh.
5. `/ui-kit` ẩn prod (env flag); registry ADR hoàn chỉnh.

## Boundary (KHÔNG làm)

- KHÔNG đụng backend services (SF-1/2 đã xong — sweep chỉ FE).
- KHÔNG đụng `packages/{auth,contracts}` logic.
- KHÔNG tạo trang content mới (footer trim — N5).
- KHÔNG đụng 2 vite configs Set-Cookie + e2e/ (SF-1 own).
