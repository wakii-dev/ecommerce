# SF-1 Context Pack — design-foundation-motion

> Đọc file này THAY VỀ tự tổng hợp. Epic spec: `docs/superpowers/specs/2026-09-08-uiux-elevation-design.md` · Bracket: `docs/superpowers/brackets/fi390-uiux-elevation.md` · Linear: FI-390 (epic) · Nhánh đích: `story/fi390-uiux-elevation`.
> **INPUT design:** `docs/superpowers/designs/fi390-uiux-elevation-direction.md` (hand-off từ designer pre-SF step — user đã chọn hướng; KHÔNG hỏi lại user về design).
> `contracts/**` READ-ONLY. Zero backend change.

## Spec slice (chỉ phần SF-1 chịu trách nhiệm)

1. **Tokens v2** (`packages/ui-kit/src/styles/tokens.css`): THÊM `--z-*` scale (dropdown/toast/modal/tooltip), breakpoint tokens (vd `--bp-sm: 600px; --bp-md: 900px`), motion tokens (`--dur-fast: 120ms; --dur-base: 180ms; --dur-slow: 250ms; --ease-*`). KHÔNG đổi hex/tên biến hiện có (Q1 brand lock; đổi value = token-regression test fail theo thiết kế → nếu direction hand-off đòi đổi thì cập nhật test + `fi310` doc reference CÙNG commit, note coordinator). 3 block theme giữ nguyên hình dạng (storefront/admin base + dark additive overlay).
2. **Font thật cho 4 app Vite**: @font-face Be Vietnam Pro (weights 400/500/600/700, subset vietnamese+latin, `display: swap`, woff2 từ Google Fonts — OFL self-host hợp lệ). Import ở CẢ HAI biên MF (quy tắc P0 — §4.4): main.tsx (standalone) + bootstrap.tsx/exposed entry (host context — bài học FI-368 T11: `mfe-checkout/src/bootstrap.tsx:6` ghi rõ main.tsx không chạy khi mount trong shell). Storefront-web ĐÃ có next/font (`app/layout.tsx:2`) — không đụng. Verify font metrics không vỡ layout (FOUT fallback `size-adjust` nếu cần).
3. **`'use client'` resolution (RSC)**: chỉ đánh dấu stateful primitives — Modal, Drawer, Tabs, Toast/ToastProvider, Select + mọi primitive mới có state/interactivity. StarRating, EmptyState, Badge, Price, Card, Skeleton giữ server-safe. Shim `storefront-web/components/ui-kit.ts` KHÔNG xóa — SF-2 thu hẹp per-primitive sau.
4. **ui-kit css distribution 5 import sites**: 4 Vite apps (`apps/{shell,mfe-checkout,mfe-account,mfe-admin}/src/main.tsx:5-6` — hiện chỉ import tokens.css) + standalone/bootstrap dual boundary. Barrel `packages/ui-kit/src/index.ts` export đủ primitives mới.
5. **Primitives mới** (CSS `uk-*` trong ui-kit.css; cấm hex literal — chỉ var(--*)): **QuantityStepper** (−/value/+, min/max/clamp, aria-label, thay hand-roll `mfe-checkout/src/pages/CartPage.tsx:51-75`); **Pagination** (URL-driven variant: prev/next/numbered + `href` builder cho SEO; client variant onPageChange; thay `storefront-web/components/plp/Pagination.tsx` + 4 admin pages "← Trước/Sau"); **Breadcrumbs** (items + aria + separator, JSON-LD render helper); **IconButton** (icon + aria-label bắt buộc + size/variant); **Alert/Banner** (info/success/warning/danger tint theo tint palette); **form controls**: Checkbox, Radio (+group), Textarea (label/error/hint/aria khớp Input.tsx pattern `useId`); **Stepper** (steps + current + done click-able/onKeyDown + connector, aria-current="step" — keyboard OK, sửa nguyên nhân stepper cũ `role=button` không onKeyDown).
6. **SVG Icon set**: `Icon` component + catalog inline SVG (stroke-based, currentColor, size prop): cart, user, heart, search, sun, moon, check, x/close, chevron-down/left/right, star, trash, plus, minus, alert, info, external, logout, settings, package, ticket. Thay emoji (🛒☀️🌙✓⚠️🎟️🛍️) ở surface SFs sau. Icon KHÔNG `'use client'` (render thuần).
7. **Skeleton compositions**: ProductCardSkeleton, TableSkeleton (rows/cols), ListSkeleton — dùng Skeleton primitive + shimmer keyframes có sẵn (`uk-skeleton-pulse` ui-kit.css:685-717); reserve đúng chiều cao (CLS).
8. **Motion utilities**: keyframes CHỈ trong ui-kit.css (cấm copy sang page.css); đã có `uk-toast-in/uk-pop-in/uk-fade-in/uk-skeleton-pulse` — dùng lại; THÊM scroll-reveal utility (`.uk-reveal` + IntersectionObserver hook `useReveal` — fade+translateY nhỏ, 1 lần, reduced-motion → hiện ngay); **global `@media (prefers-reduced-motion: reduce)`** trong ui-kit.css: animation/transition-duration ≈ 0 (0.01ms), reveal hiện ngay.
9. **i18n contract**: keys cho primitives mặc định (vd Pagination prev/next/pageOf, Breadcrumbs aria, Stepper step, QuantityStepper increase/decrease) vào `packages/i18n/src/catalogs/{vi,en}.ts` namespace mới additive (vd `ui.*`), vi+en song song, parity test; **data-testid convention ADR** (`docs/adr/` — semantic classname giữ + `data-testid="<surface>-<element>"` khi rename bất khả kháng; e2e specs dùng ~30 classname selector).
10. **Tabs keyboard**: Tabs ĐÃ có roving tabindex + arrow-key (Tabs.tsx:39-55) — chỉ thêm unit test KHÓA hành vi (arrow wrap, Home/End nếu có), không sửa logic trừ khi test bộc lộ bug.
11. **Unit tests**: primitives mới (SSR renderToStaticMarkup pattern `__tests__/uiKit.test.tsx` + jsdom interaction pattern `overlay.test.tsx`) + token-regression cập nhật + Tabs keyboard test. Mọi test phải xanh.
12. **Design tokens từ hand-off**: mọi spacing/radius/shadow/typography mà direction hand-off chỉ định → áp vào tokens.css + ui-kit.css (trong brand lock — hex primary giữ).

## Touch map (files SF-1 tạo/sở hữu)

```
frontend/packages/ui-kit/src/styles/tokens.css        (sở hữu — thêm, không đổi hex cũ)
frontend/packages/ui-kit/src/styles/ui-kit.css        (sở hữu — css primitives mới + motion + reduced-motion)
frontend/packages/ui-kit/src/components/*             (sở hữu — primitives mới + 'use client' đánh dấu)
frontend/packages/ui-kit/src/index.ts                 (sở hữu — barrel exports)
frontend/packages/ui-kit/src/__tests__/*              (sở hữu — tests mới + token regression)
frontend/packages/i18n/src/catalogs/{vi,en}.ts        (sở hữu — namespace ui.* additive)
apps/{shell,mfe-checkout,mfe-account,mfe-admin}/src/main.tsx      (đụng 1 dòng: import ui-kit css)
apps/{shell,mfe-checkout,mfe-account,mfe-admin}/src/bootstrap.tsx (đụng 1 dòng: import ui-kit css — host biên)
apps/shell/src/pages/UiKitDemoPage.tsx                (sở hữu — demo compose THỦ CÔNG, primitive mới phải thêm vào đây — acceptance #2)
docs/adr/*                                            (sở hữu — data-testid convention ADR)
```

**Intra-SF thứ tự gợi ý (DAG nội bộ):** tokens v2 TRƯỚC mọi primitive task (primitives dùng var mới) · font + ui-kit-css-import SERIALIZE với nhau (cùng đụng 4×2 file main/bootstrap) · i18n-contract TRƯỚC primitives dùng keys `ui.*` · unit-tests CUỐI.
READ-ONLY: `contracts/**`, `backend/**`, `apps/storefront-web/**` (SF-2 own — trừ ghi chú shim), `apps/*/src/pages/**` (surface SFs own), e2e/ (SF-6 own), pnpm-lock (dep freeze).

## ACCEPTANCE (user-visible)

1. Mở storefront-web home + shell-mounted checkout: font Be Vietnam Pro hiển thị đúng trên CẢ 2 context (standalone và trong shell) — computed font-family khớp.
2. `/ui-kit` demo (shell, env VITE_UIKIT_DEV=1): thấy toàn bộ primitives mới hoạt động — stepper bấm được bằng keyboard, pagination chuyển trang, alert/checkbox/radio/textarea đúng tint, icon set hiện thay emoji.
3. Toast/pop/skeleton có animation; cuộn trang có reveal section; bật `prefers-reduced-motion` (OS setting) → mọi animation tắt, nội dung vẫn hiện đủ.
4. Dark mode + admin theme: primitives render đúng cả 4 trạng thái (storefront/admin × light/dark), không hex lệch.
5. `pnpm vitest run` trong packages/ui-kit + i18n: xanh (tests mới + regression).

## Boundary (KHÔNG làm)

- KHÔNG đụng `apps/storefront-web/**` pages/components (SF-2) — kể cả shim thu hẹp (SF-2 own per-primitive).
- KHÔNG đụng pages của checkout/account/admin (SF-3/4/5) — chỉ import dòng css ở main/bootstrap.
- KHÔNG chạy e2e Playwright (SF-6); KHÔNG backend; KHÔNG thêm dependency mới (dep freeze — scroll-reveal hand-roll IntersectionObserver, không react-intersection-observer).
- KHÔNG đổi hex/tên token hiện có; KHÔNG đổi MF remote names/HeaderSlots contract; KHÔNG merge vào main.
- Gate KHÔNG assert hiệu ứng surface (header xịn, skeleton ở orders...) — chỉ những gì SF-1 tự cung cấp (tier-gate rule).
