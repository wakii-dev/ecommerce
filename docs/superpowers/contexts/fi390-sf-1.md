# SF-1 Context Pack — design-foundation-motion

> Đọc file này THAY VỀ tự tổng hợp. Epic spec: `docs/superpowers/specs/2026-09-08-uiux-elevation-design.md` · Bracket: `docs/superpowers/brackets/fi390-uiux-elevation.md` · Linear: FI-390 (epic) · Nhánh đích: `story/fi390-uiux-elevation`.
> **INPUT design:** `docs/superpowers/designs/fi390-uiux-elevation-direction.md` (hand-off từ designer pre-SF step — user đã chọn hướng; KHÔNG hỏi lại user về design). **Mọi số liệu motion/breakpoint/gradient/z-index lấy từ hand-off §1.2–§1.5 — spec này không định nghĩa lại số.**
> `contracts/**` READ-ONLY. Zero backend change.
> Amendment 2026-09-09 sau spec-critic cycle 1 (P0×4, P1×7): authorize đầy đủ hand-off §1.3/§1.4/§1.5, weight 800, mandate test-update cho admin-dark, demo 4-state, sửa claim stale import/dag/touch-map, chốt mechanism i18n primitives.

## Spec slice (chỉ phần SF-1 chịu trách nhiệm)

1. **Tokens v2** (`packages/ui-kit/src/styles/tokens.css`): KHÔNG đổi hex/tên biến hiện có (Q1 brand lock; đổi value = token-regression test fail theo thiết kế). **THÊM đầy đủ token mới theo hand-off** (nguồn số DUY NHẤT = hand-off, không dùng số trong ngoặc của spec cũ):
   - §1.2 motion: `--dur-fast/base/slow` + `--dur-carousel/kb/shimmer/float` + `--ease-out/pop/drawer` (giá trị ĐÚNG theo hand-off §1.2 — 140/180/260ms...).
   - §1.2 z-index: `--z-header/dropdown/drawer/toast` (100/200/300/400).
   - §1.2 breakpoints: `--bp-sm/md/lg` (600/960/1240px). **LƯU Ý KỸ THUẬT: `@media` CSS KHÔNG đọc được `var(--bp-*)`** — breakpoint tokens chỉ là doc/JS reference; rule responsive trong ui-kit.css hardcode px tương ứng.
   - §1.3 gradient + CTA shadow: `--grad-cta/flash/hero-1..3`, `--grad-cat-*` (5), `--grad-swatch-*` (4), `--shadow-cta/-hover` — hex dẫn xuất family FI-310 §1.8, hợp lệ vì hand-off §1.3 liệt kê tường minh.
   - §1.4: 3 shadow value đè vào block `[data-theme='dark']` (dark hiện KHÔNG declare shadow nào — cascade từ :root). Format rgba theo convention tokens.css (`0.4` có leading zero — không `.4` như hand-off viết).
   - §1.5: block mới `[data-theme='admin-dark']` (hex mới DUY NHẤT được phép của cả elevation: `#0F0F0F`; còn lại copy đúng block dark).
   - **Cập nhật token-regression test + doc reference CÙNG commit** — bao gồm cả VIỆC THÊM `admin-dark` + §1.4 (hand-off ghi "THÊM block/var mới = an toàn" là KHÔNG ĐÚNG: `tokens.test.ts` có 7 assertion exact-count vỡ khi admin-dark duplicate giá trị shared — warning/accent/on-accent 3→4; link-dark/text-muted-dark/border-dark/danger-dark 1→2). 3 block theme cũ giữ nguyên hình dạng.
2. **Font thật cho 4 app Vite**: @font-face Be Vietnam Pro weights **400/500/600/700/800** (hand-off §1.6 + header tokens.css + anatomy §2 dùng 800 khắp nơi — 4 weights là thiếu hợp đồng font cho SF-2..5), subset vietnamese+latin, `display: swap`, woff2 self-host từ Google Fonts (OFL) vào `packages/ui-kit/src/assets/fonts/`, **@font-face đặt trong tokens.css** (url() relative — Vite resolve tự động ở mọi import site, phủ CẢ HAI biên MF mà không cần thêm import point nào). Storefront-web ĐÃ có next/font (`app/layout.tsx:2`) — không đụng (next/font dùng hashed family name riêng, không xung đột @font-face 'Be Vietnam Pro'). Verify font metrics không vỡ layout (FOUT fallback `size-adjust` nếu cần).
3. **`'use client'` resolution (RSC)**: chỉ đánh dấu stateful primitives — Modal, Drawer, Tabs, Toast/ToastProvider, Select + mọi primitive mới có state/interactivity. StarRating, EmptyState, Badge, Price, Card, Skeleton, **Icon** giữ server-safe. Shim `storefront-web/components/ui-kit.ts` KHÔNG xóa KHÔNG sửa — SF-2 thu hẹp per-primitive sau.
4. **ui-kit css distribution — 2 biên MF, 7 file**: 4 `main.tsx` (shell/checkout/account/admin) **ĐÃ import cả tokens.css + styles.css** (không phải việc — spec cũ ghi "hiện chỉ import tokens.css" là stale). **Việc thật = 3 `bootstrap.tsx`**: mfe-checkout + mfe-account hiện chỉ import page.css riêng; **mfe-admin hiện KHÔNG import css nào** — cả 3 thêm import `@ecommerce/ui-kit/tokens.css` + `@ecommerce/ui-kit/styles.css` (pattern FI-368 T11; dedupe vô hại khi standalone). Shell là host, KHÔNG có bootstrap.tsx. Barrel `packages/ui-kit/src/components/index.ts` export đủ primitives mới (thêm theo từng primitive task).
5. **Primitives mới** (CSS `uk-*` trong ui-kit.css theo BEM pattern hiện có; cấm hex literal — chỉ var(--*)): **QuantityStepper** (−/value/+, min/max/clamp, aria-label); **Pagination** (URL-driven variant: prev/next/numbered + window ±ellipsis + `href` builder prop cho SEO; client variant `onPageChange`); **Breadcrumbs** (items + aria + separator, JSON-LD render helper); **IconButton** (icon + aria-label bắt buộc + size/variant); **Alert/Banner** (info/success/warning/danger tint theo tint palette); **form controls**: Checkbox, Radio (+group), Textarea (label/error/hint/aria khớp Input.tsx pattern `useId`); **Stepper** (steps + current + done, nút thật click-able + onKeyDown + connector, `aria-current="step"` — sửa nguyên nhân stepper cũ `role=button` không onKeyDown). **LƯU Ý PHÂN ĐỊNH: SF-1 CHỈ cung cấp primitive — việc THAY hand-roll là của surface SFs**: SF-3 thay `CartPage.tsx:51-79` qty stepper; SF-2 thay `storefront-web/components/plp/Pagination.tsx`; SF-5 thay pagination "← Trước/Sau" 4 admin pages. SF-1 KHÔNG đụng các file đó.
6. **SVG Icon set**: `Icon` component + catalog inline SVG (stroke **1.8**, `currentColor`, size prop default 20, range 18–22 — hand-off §6): cart, user, heart, search, sun, moon, check, x/close, chevron-down/left/right, star, trash, plus, minus, alert, info, external, logout, settings, package, ticket. SF-2..5 sẽ thay emoji bằng Icon (SF-1 không thay). Icon KHÔNG `'use client'` (render thuần).
7. **Skeleton compositions**: ProductCardSkeleton, TableSkeleton (rows/cols), ListSkeleton — dùng Skeleton primitive; **nâng look shimmer theo hand-off §2.5**: gradient sweep wash→border→wash (`background-size 240%`, quét 120%→−120%), duration `var(--dur-shimmer)` 1.3s linear — thay look pulse cũ (SF-1 sở hữu loading pattern chung; giữ tên class/keyframe `uk-skeleton-pulse`, đổi rule bên trong); reserve đúng chiều cao (CLS).
8. **Motion utilities**: keyframes CHỈ trong ui-kit.css (cấm copy sang page.css); đã có `uk-toast-in/uk-pop-in/uk-fade-in/uk-skeleton-pulse` — dùng lại; THÊM scroll-reveal utility (`.uk-reveal` + hook `useReveal` — **progressive enhancement: content hiện theo mặc định, JS pre-hide rồi reveal** — chống SSR/no-JS invisible; fade + translateY(18px→0) .5s `--ease-out`, IntersectionObserver threshold 0.12, stagger 70ms, unobserve sau khi hiện, 1 lần; reduced-motion → hiện ngay); **global `@media (prefers-reduced-motion: reduce)`** trong ui-kit.css ĐỦ recipe hand-off §3.3: `animation-duration: .01ms !important; animation-iteration-count: 1 !important; transition-duration: .01ms !important; scroll-behavior: auto` + reveal hiện ngay (page.css hiện 0 keyframes — an toàn; transitions instant-complete không freeze mid-state).
9. **i18n contract**: keys `ui.*` namespace additive trong `packages/i18n/src/catalogs/{vi,en}.ts` (Pagination prev/next/pageOf, Breadcrumbs aria, Stepper step/bước, QuantityStepper increase/decrease...) — vi+en song song + parity test (structural so keys vi↔en). **MECHANISM CHỐT (dep freeze — ui-kit KHÔNG thêm dep @ecommerce/i18n): primitives KHÔNG gọi useT — nhận label/aria qua props với default tiếng Việt khớp string của keys `ui.*`; surfaces SF-2..5 truyền `t('ui.*')` vào props khi cần override.** Keys tồn tại để surfaces tiêu thụ + làm hợp đồng đặt tên. **ADR data-testid** (`docs/adr/` — semantic classname giữ + `data-testid="<surface>-<element>"` khi rename bất khả kháng; e2e specs dùng ~30 classname selector).
10. **Tabs keyboard**: Tabs ĐÃ có roving tabindex + arrow-key (Tabs.tsx:40-80) — chỉ thêm unit test KHÓA hành vi (arrow wrap, focus move), không sửa logic trừ khi test bộc lộ bug.
11. **Unit tests**: primitives mới (SSR renderToStaticMarkup pattern `__tests__/uiKit.test.tsx` + jsdom interaction pattern `components/__tests__/overlay.test.tsx`) — **mỗi primitive task tự kèm test của nó**; Tabs keyboard test + token-regression cập nhật (ở task 1) + consolidation full-suite xanh ở task cuối. Mọi test phải xanh.
12. **Design tokens từ hand-off**: mọi spacing/radius/shadow/**gradient**/typography mà hand-off chỉ định → áp vào tokens.css + ui-kit.css (trong brand lock — hex primary giữ; hex mới chỉ #0F0F0F + gradient family đã liệt kê §1.3).

## Touch map (files SF-1 tạo/sở hữu)

```
frontend/packages/ui-kit/src/styles/tokens.css        (sở hữu — thêm tokens v2 + @font-face, không đổi hex cũ)
frontend/packages/ui-kit/src/assets/fonts/*.woff2     (mới — 10 file: 5 weights × vietnamese+latin, OFL)
frontend/packages/ui-kit/src/styles/ui-kit.css        (sở hữu — css primitives mới + motion + reduced-motion + shimmer)
frontend/packages/ui-kit/src/components/*             (sở hữu — primitives mới + 'use client' đánh dấu)
frontend/packages/ui-kit/src/components/index.ts      (sở hữu — barrel exports)
frontend/packages/ui-kit/src/demo/UiKitDemo.tsx       (sở hữu — sections primitives mới + switcher MỞ RỘNG 4 theme states storefront/admin × light/dark + reveal demo section — acceptance #2/#3/#4)
frontend/packages/ui-kit/src/__tests__/*              (sở hữu — tests mới + token regression)
frontend/packages/i18n/src/catalogs/{vi,en}.ts        (sở hữu — namespace ui.* additive)
frontend/packages/i18n/src/__tests__/i18n.test.ts     (sở hữu — parity test structural)
frontend/apps/mfe-{checkout,account,admin}/src/bootstrap.tsx  (đụng 2 dòng: import ui-kit css — host biên; shell KHÔNG có bootstrap.tsx)
frontend/apps/shell/src/pages/UiKitDemoPage.tsx       (1-line wrapper — không cần đổi; demo content qua @ecommerce/ui-kit/demo)
docs/adr/*                                            (sở hữu — data-testid convention ADR, số tiếp theo sau 0006)
```

**Intra-SF thứ tự gợi ý (DAG nội bộ):** tokens v2 TRƯỚC mọi primitive task (primitives dùng var mới) · i18n-contract TRƯỚC primitives dùng keys `ui.*` · **SERIALIZE: mọi primitive task đụng chung ui-kit.css / components/index.ts / UiKitDemo.tsx → các task primitive chạy GẦN-NHẤT-Serial (không parallel worker cùng đụng 3 file này — commit-race hazard)** · font task sau tokens task (cùng file tokens.css) · unit-tests CUỐI.
READ-ONLY: `contracts/**`, `backend/**`, `apps/storefront-web/**` (SF-2 own — kể cả shim), `apps/*/src/pages/**` (surface SFs own), e2e/ (SF-6 own), pnpm-lock (dep freeze).

## ACCEPTANCE (user-visible)

1. Mở storefront-web home + shell-mounted checkout: font Be Vietnam Pro hiển thị đúng trên CẢ 2 context (standalone và trong shell) — computed font-family khớp.
2. `/ui-kit` demo (shell, env VITE_UIKIT_DEV=1): thấy toàn bộ primitives mới hoạt động — stepper bấm được bằng keyboard, pagination chuyển trang, alert/checkbox/radio/textarea đúng tint, icon set hiện thay emoji placeholder, **switcher theme đủ 4 trạng thái**.
3. Toast/pop/skeleton có animation; cuộn trang có reveal section (demo section riêng); bật `prefers-reduced-motion` (OS setting) → mọi animation tắt, nội dung vẫn hiện đủ.
4. Dark mode + admin theme: primitives render đúng cả 4 trạng thái (storefront/admin × light/dark — verify qua demo switcher 4-state, admin-dark = block §1.5).
5. `pnpm vitest run` trong packages/ui-kit + i18n: xanh (tests mới + regression).

## Boundary (KHÔNG làm)

- KHÔNG đụng `apps/storefront-web/**` pages/components (SF-2) — kể cả shim thu hẹp (SF-2 own per-primitive).
- KHÔNG đụng pages của checkout/account/admin (SF-3/4/5) — chỉ import dòng css ở bootstrap.tsx (main.tsx đã có sẵn).
- KHÔNG chạy e2e Playwright (SF-6); KHÔNG backend; KHÔNG thêm dependency mới (dep freeze — scroll-reveal hand-roll IntersectionObserver, không react-intersection-observer; primitives KHÔNG phụ thuộc i18n — label qua props).
- KHÔNG đổi hex/tên token hiện có; KHÔNG đổi MF remote names/HeaderSlots contract; KHÔNG merge vào main.
- Gate KHÔNG assert hiệu ứng surface (header xịn, skeleton ở orders...) — chỉ những gì SF-1 tự cung cấp (tier-gate rule).
