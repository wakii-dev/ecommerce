# SF-2 Context Pack — storefront-web-elevation

> Đọc file này THAY VỀ tự tổng hợp. Epic spec: `docs/superpowers/specs/2026-09-08-uiux-elevation-design.md` · Bracket: `docs/superpowers/brackets/fi390-uiux-elevation.md` · Linear: FI-390 (epic) · Nhánh đích: `story/fi390-uiux-elevation`.
> **INPUT design:** `docs/superpowers/designs/fi390-uiux-elevation-direction.md` (đã duyệt — SF-2 inherit, KHÔNG hỏi user). SF-1 đã merge primitives/tokens/motion vào nhánh đích — CONSUMER.
> `contracts/**` READ-ONLY. Zero backend change. SEO infra ĐÃ TỐT — không làm lại (JSON-LD Product, hreflang, canonical, sitemap, OG, noindex-en-duplicate, PWA).

## Spec slice (chỉ phần SF-2 chịu trách nhiệm)

1. **next/link migration**: 24 chỗ `<a href>` thô → `next/link` (Link render `<a>` thật — no-JS fallback GIỮ; prefetch mặc định, giới hạn ở links filter/sort tham số dài). SortSelect `window.location.assign` → `router.push(scroll:false)`. PLP filter/sort/pagination VẪN URL-driven (cấm đẩy vào client state — vỡ canonical/hreflang + no-JS). Kiểm: mọi internal anchor trong `apps/storefront-web` qua `<Link>`.
2. **loading/error/not-found**: thêm `loading.tsx` + `error.tsx` cho home/PLP/PDP/search (streaming SSR — skeleton compositions từ SF-1: ProductCardSkeleton cho grid, reserve chiều cao chống CLS); `error.tsx` tiếng locale, không crash Next mặc định; elevate `not-found.tsx` (hiện text-only trộn vi/en → design + i18n đúng).
3. **Header polish per direction**: search focus ring, mini-nav (hiện 3 link đều trỏ /c/dien-tu — thay bằng root categories thật nếu API sẵn, hoặc giữ nhưng polish visual), cart-badge link, actions spacing.
4. **Hero carousel elevation**: ken-burns/parallax NHẸ trên slide (Q2 B, chỉ hero), pause on hover/focus + `prefers-reduced-motion` tôn trọng (sửa a11y: autoplay không pause), dots/arrows per direction.
5. **Home sections + scroll-reveal**: FlashDeal/CategoryTiles/featured grid áp direction hand-off; scroll-reveal qua utility SF-1 (`useReveal`/`.uk-reveal`) — 1 lần, reduced-motion safe; hover states card chuẩn (giữ hover-lift có sẵn, chuẩn hóa duration qua motion tokens).
6. **ProductCardView consolidate**: giá + discount badge tự render (ProductCardView.tsx:100-105) → dùng `Price` primitive SF-1 consolidate; hover-lift + wishlist heart polish (scale, reduced-motion).
7. **PLP**: Pagination primitive (URL-driven variant) thay `components/plp/Pagination.tsx`; filter sidebar "checkbox giả" span trong `<a>` (Sidebar.tsx:74-76) → expose state cho AT (aria-pressed/checkbox thật); Toolbar/SortSelect polish.
8. **PDP**: Gallery hover-zoom (Q2 B — chỉ gallery) + polish; tabs CSS `:target` (page.tsx:267-273) → tabs thật keyboard (ui-kit Tabs — LƯU Ý: cần client boundary; shim `components/ui-kit.ts` thu hẹp per-primitive — SF-2 own, thêm Tabs/Modal vào shim khi wire); BuyBox sticky mobile; AddToCart toast → ui-kit Toast (bỏ `.pdp-toast` tự viết) giữ nguyên error honesty logic (toast lỗi thật — FI-372).
9. **PDP review modal**: `WriteReviewModal.tsx` (210 dòng tự viết, thiếu focus-trap/ESC/restore) → ui-kit Modal (focus-trap/ESC/restore có sẵn useOverlay); giữ lifecycle 409 duplicate + guest CTA.
10. **Search + Coupons polish**: empty states + chips gợi ý per direction; CopyButton en render "Copy" cả 2 locale (CopyButton.tsx:59) → i18n đúng.
11. **Footer + NewsletterForm + RecentlyViewed**: NewsletterForm hex cứng (#bbb/#7ed957/#222/#444/#F53D2D — NewsletterForm.tsx:61-104) → tokens; RecentlyViewed card inline-style toàn bộ (RecentlyViewed.tsx:44-82) → css class + dùng `.p-card` anatomy; footer polish per direction.
12. **i18n consolidate**: 12+ file giữ `Record<'vi'|'en'>` COPY riêng lẻ (Header, SearchBar, PDP page, search, coupons, AddToCart, StockAlert, WriteReviewModal, MyPending, WriteReviewControl, WishlistHeart, NewsletterForm, RecentlyViewed...) → gom 1 module i18n nội bộ storefront (không ép qua @ecommerce/i18n — storefront dùng [locale] routing; giữ pattern COPY nhưng tập trung + type-safe); ~10 aria-label hardcoded vi → keys vi/en.
13. **Responsive <600px**: mobile nav (hamburger/scroll-x mini-nav per direction), hero cao co giãn, featured grid 2-col, sticky AddToCart bar mobile PDP; thêm breakpoint từ tokens SF-1 (`--bp-sm`).
14. **SEO cộng mới + placeholder fix**: thêm BreadcrumbList JSON-LD (khoảng trống SEO duy nhất — lib/pdp.ts pattern); "Đã bán" hiển thị ratingCount (p/[slug]:242) → đổi label thành "N đánh giá" (dữ liệu thật, hết nói dối).
15. **Walkthrough + e2e subset**: browser walkthrough 5 màn (home/PLP/PDP/search/coupons) + screenshots record; chạy e2e subset (gate §6): golden-path, nav-honesty, related-products, review-flow — XANH (selectors: giữ semantic classname; rename bất khả kháng → data-testid song song theo ADR SF-1).

## Touch map (files SF-2 tạo/sở hữu)

```
apps/storefront-web/app/**                    (sở hữu — pages, layout, loading/error mới)
apps/storefront-web/app/app.css               (sở hữu — section theo trang; surface keyframes ĐƯỢC PHÉP ở đây)
apps/storefront-web/components/**             (sở hữu toàn bộ)
apps/storefront-web/components/ui-kit.ts      (sở hữu — shim thu hẹp per-primitive)
apps/storefront-web/lib/{seo,pdp,format}.ts   (đụng — BreadcrumbList JSON-LD + label fix)
```
READ-ONLY: `packages/ui-kit/**` (SF-1 own — bug primitive → flag coordinator, KHÔNG sửa), `packages/i18n/**` (không dùng ở storefront), `contracts/**`, `backend/**`, shell/MFEs (SF-3/4/5 own), e2e/ (read-only, chạy subset).

## ACCEPTANCE (user-visible)

1. Bấm sort/filter/pagination/link trên home→PLP→PDP→search→coupons: KHÔNG có trắng trang reload — chuyển mượt, URL đúng, share/bookmark URL vẫn đúng nội dung (no-JS vẫn chạy được GET form).
2. Chặn network chậm: thấy skeleton thay trang trắng; simulate lỗi → error page đẹp tiếng Việt/Anh, không crash Next.
3. PDP: tab chuyển bằng chuột + arrow-key, không jump-scroll; modal review chặn focus trong modal, ESC đóng, focus trở lại nút mở; gallery hover phóng ảnh nhẹ; mobile <600px có sticky AddToCart, grid 2-col không vỡ.
4. Mọi text/aria-label có đủ vi + en (đổi locale → không còn tiếng lẫn); không còn emoji làm icon trong storefront chrome.
5. e2e subset (golden-path, nav-honesty, related-products, review-flow) XANH + unit tests storefront xanh.

## Boundary (KHÔNG làm)

**E2E port base: +0** (mock-gateway :9099 + alt-ports pattern fe-e2e-isolated — SF-3 +100, SF-4 +200, SF-5 +300 để 4 stack song song không port war). Task t8 (gallery+tabs+sticky+mobile) lớn — Phase 3 NÊN tách 2 (pdp-gallery-zoom + pdp-tabs-sticky-mobile).

- KHÔNG sửa `packages/ui-kit/**` (SF-1 own — primitive thiếu gì → flag coordinator).
- KHÔNG đụng shell/MFEs (SF-3/4/5), backend, contracts; KHÔNG đổi SEO hiện có (chỉ CỘNG BreadcrumbList).
- KHÔNG scroll-reveal ngoài home sections; KHÔNG đổi routes/URL pattern; KHÔNG đổi logic AddToCart/wishlist/coupon (chỉ presentation quanh chúng — honesty logic giữ nguyên).
- KHÔNG chạy FULL e2e suite (SF-6); KHÔNG merge vào main.
