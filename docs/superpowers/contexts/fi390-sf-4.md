# SF-4 Context Pack — account-elevation

> Đọc file này THAY VỀ tự tổng hợp. Epic spec: `docs/superpowers/specs/2026-09-08-uiux-elevation-design.md` · Bracket: `docs/superpowers/brackets/fi390-uiux-elevation.md` · Linear: FI-390 (epic) · Nhánh đích: `story/fi390-uiux-elevation`.
> **INPUT design:** `docs/superpowers/designs/fi390-uiux-elevation-direction.md` (đã duyệt — inherit). SF-1 đã merge primitives/tokens/motion — CONSUMER.
> `contracts/**` READ-ONLY. Zero backend change. KHÔNG đụng logic auth (2FA challenge, OAuth env-gated, authReady race fix `AccountPage.tsx:21-48`) — chỉ presentation quanh chúng.

## Spec slice (chỉ phần SF-4 chịu trách nhiệm)

1. **Account layout side-nav**: hiện mỗi tính năng 1 route lẻ, /account chỉ 2 card cạnh nhau (AccountPage.tsx:86-145); điều hướng wishlist/reviews CHỈ qua footer storefront (Footer.tsx:44-45) → layout chung side-nav (Tài khoản / Đơn hàng / Wishlist / Đánh giá của tôi / Affiliate / Điểm thưởng) + active state + icon SVG + responsive collapse (<600px → tabs ngang/scroll-x); route hiện có GIỮ NGUYÊN (không đổi URL pattern).
2. **Auth flows polish**: Login/Register/Forgot/Reset/2FA forms per direction — validate realtime on-blur, password visibility toggle, inline error (ui-kit Input error state có sẵn), spacing/typography direction; KHÔNG password strength meter (overkill); OAuth buttons env-gated giữ nguyên.
3. **UserMenu dropdown keyboard**: AuthWidget.tsx:30-36,56-76 — span-based, không role=menuitem, không keyboard, đóng chỉ outside-click → menu chuẩn (role=menu/menuitem, Escape đóng, arrow-key điều hướng, focus restore); Icon thay ▲/▼ (AuthWidget.tsx:64).
4. **AccountPage profile + 2FA**: polish form Họ tên/SĐT (validate realtime, inputMode tel), section 2FA (TwoFactorSection) per direction, email/role read-only hiển thị sạch.
5. **OrdersPage**: card list polish + status pill 6 màu chuẩn tokens + ListSkeleton thay text "Đang tải…" (OrdersPage.tsx:76) + empty state elevate.
6. **OrderDetailPage decss** (396 dòng, 42 khối inline style): → page.css classes; `<table>` tay + inline border (~210-230) → ui-kit Table; RMA modal + hủy đơn modal → ui-kit Modal (focus-trap/ESC); raw `<textarea>` (~350-365) → Textarea primitive SF-1; tracking GHN timeline polish; invoice PDF button/icon.
7. **WishlistPage**: grid nhất quán ProductCard anatomy (giờ tự render — WishlistPage.tsx); nút Xóa → confirm + Icon; ListSkeleton thay "Đang tải…" (WishlistPage.tsx:81); empty state 🛍️ → EmptyState + icon.
8. **MyReviewsPage**: list review + badge PENDING/APPROVED/REJECTED → Badge primitive tints; polish per direction.
9. **Affiliate + Loyalty polish**: AffiliatePage.tsx (299 dòng, 16 inline style) stats cards + ref-link generator + ledger; LoyaltyPointsSection điểm; decss → classes + tokens.
10. **i18n**: hard-code tiếng Việt trong JSX account → keys `@ecommerce/i18n` — **CHỈ namespace `account.*` additive-only**; **ANCHOR (P1 — catalogs là single-file object, 3 SF song song):** chèn block `account:` NGAY SAU block `nav:` trong cả vi.ts + en.ts — SF-3 chèn `checkout:`/`shell:` sau `auth:`, SF-5 chỉ sửa trong `admin:` — khác anchor = merge sạch; vi+en song song.
11. **Responsive + walkthrough + tests**: <600px side-nav collapse + forms 1-col; walkthrough login→side-nav→orders→order-detail→wishlist→reviews→affiliate + dark mode; e2e subset (gate §6): auth-cookie, password-reset, engagement — XANH; unit mfe-account xanh.

## Touch map (files SF-4 tạo/sở hữu)

```
apps/mfe-account/src/**                      (sở hữu toàn bộ — pages/AuthWidget/AccountLayout mới)
apps/mfe-account/src/page.css                (sở hữu — surface keyframes OK; hex #FDEBEC/#C0151F pin có comment — giữ trừ khi direction chỉ định)
packages/i18n/src/catalogs/{vi,en}.ts        (đụng — CHỈ thêm namespace account.* additive)
```
READ-ONLY: `packages/ui-kit/**` (SF-1 own — bug → flag), `packages/auth/**` (logic — KHÔNG đụng), `apps/shell/**` (SF-3 own HeaderSlots — consume slot có sẵn), `apps/mfe-checkout/**` (SF-3), `apps/storefront-web/**` (SF-2), `contracts/**`, `backend/**`, e2e/ (read-only, chạy subset). pnpm-lock dep freeze.

## ACCEPTANCE (user-visible)

1. Login xong → menu tài khoản → trang account có side-nav đầy đủ 6 mục, active state rõ; mọi trang account vào được từ side-nav (không còn chỉ qua footer).
2. Loading mọi trang account là skeleton (không còn chữ "Đang tải…"); đơn hàng hiện status pill màu chuẩn; wishlist/cards nhìn nhất quán với storefront.
3. Order detail: bảng hàng hóa/table, modal RMA + hủy đơn chặn focus + ESC đóng; nhập lý do RMA là textarea có label/đếm; timeline vận chuyển dễ đọc.
4. UserMenu: mở bằng keyboard, điều hướng arrow-key, ESC đóng, focus trở lại; mọi form validate ngay khi blur, lỗi inline đỏ đúng tint, mật khẩu có nút hiện/ẩn.
5. Đổi locale en: toàn bộ account text chuyển tiếng Anh (không còn hard-code vi).
6. e2e subset (auth-cookie, password-reset, engagement) XANH + unit mfe-account xanh.

## Boundary (KHÔNG làm)

**E2E port base: +200** (mock-gateway :9099+200 + alt-ports — SF-2 +0, SF-3 +100, SF-5 +300).

- KHÔNG sửa `packages/ui-kit/**`, `packages/auth/**` (bug → flag coordinator); KHÔNG đụng logic auth/2FA/OAuth/merge-cart.
- KHÔNG đổi route/URL pattern account; KHÔNG sổ địa chỉ (Q3 — không có màn addresses trong scope này).
- KHÔNG key i18n ngoài `account.*` (SF-3 own `checkout.*`); KHÔNG sửa shell Header (SF-3) — AuthWidget đăng ký slot sẵn; KHÔNG FULL e2e (SF-6); KHÔNG merge vào main.
