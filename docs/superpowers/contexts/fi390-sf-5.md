# SF-5 Context Pack — admin-elevation

> Đọc file này THAY VỀ tự tổng hợp. Epic spec: `docs/superpowers/specs/2026-09-08-uiux-elevation-design.md` · Bracket: `docs/superpowers/brackets/fi390-uiux-elevation.md` · Linear: FI-390 (epic) · Nhánh đích: `story/fi390-uiux-elevation`.
> **INPUT design:** `docs/superpowers/designs/fi390-uiux-elevation-direction.md` (đã duyệt — inherit; admin theme block giữ shape). SF-1 đã merge primitives/tokens/motion — CONSUMER.
> `contracts/**` READ-ONLY. Zero backend change. KHÔNG đụng logic react-query invalidate, order state-machine guard (`lib/orderRules.ts`), RBAC boot race fix (`AdminApp.tsx:144-163`), CSV export, invoice PDF — chỉ presentation quanh chúng. Q5 B: client-side sort/page-size CÓ; server-side sort/filter/bulk KHÔNG.

## Spec slice (chỉ phần SF-5 chịu trách nhiệm)

1. **AdminShell sidebar**: polish per direction — icon SVG cho mỗi nav item (Icon set SF-1), active state rõ, group nav (Tổng quan / Sản phẩm / Đơn hàng / Khách hàng & tương tác / Hệ thống), spacing/typography tokens; KHÔNG đụng RBAC guard.
2. **DashboardPage**: KPI cards polish (tint/shadow theo direction), recharts colors đã override qua token (page.css:349-360 — giữ pattern), low-stock table polish.
3. **Tables client-side UX (Q5 B)**: sort cột client-side (click header → asc/desc/none, aria-sort, chỉ sort trang hiện tại — data đã load client), page-size selector (10/25/50), sticky header, row hover, TableSkeleton (SF-1) khi loading; áp cho tables lớn: ProductsList, Orders, Coupons, Affiliates, Audit, Newsletter, Reviews, RMA, Loyalty.
4. **Pagination primitive swap**: "← Trước/Sau" (ProductsListPage.tsx:221-236 + 3 pages khác) → Pagination primitive SF-1 (client variant) + keys `admin.common.*` hiện có.
5. **ProductsList + ProductForm polish**: filter bar polish (search/status/category — client-side hiện trạng giữ nguyên), table density; ProductFormPage (590 dòng): form grid thoáng, upload ảnh area đẹp hơn (dropzone-style visual, KHÔNG đổi upload logic input file), variants grid (page.css:232-239) polish, tabs vi/en dùng Tabs primitive đẹp hơn.
6. **Orders + OrderDetail admin**: status pill 6 màu chuẩn + timeline hành động + action buttons (ship/deliver/cancel) polish + confirm dialog nhất quán.
7. **8 màn còn lại visual pass**: Categories, Coupons (form modal), Affiliates, Audit, Newsletter, Loyalty, Reviews, RMA — spacing/pills/empty states/headers theo direction; KHÔNG đụng CRUD logic. (Task to — Phase 3 NÊN tách 2×4 nếu executor hay hết budget.)
8. **Cleanup**: `admin-badge-mock` class mock còn sót (page.css:195-203) → xóa; sweep hex literal trong page.css → var(--*) (trừ pin có comment); admin theme block tokens giữ nguyên shape.
9. **Forbidden + error states**: ForbiddenPage polish; error/toast states nhất quán tint.
10. **Walkthrough 4 trạng thái theme + tests**: walkthrough toàn 14 màn (storefront-admin × light/dark = 4 trạng thái) + screenshots; e2e subset (gate §6): admin-coupon, admin-crud, upload-image, rbac — XANH; unit mfe-admin xanh (fix test vỡ do polish).

## Touch map (files SF-5 tạo/sở hữu)

```
apps/mfe-admin/src/**                        (sở hữu toàn bộ — AdminShell/14 pages/lib nếu cần)
apps/mfe-admin/src/page.css                  (sở hữu — surface keyframes OK; cleanup admin-badge-mock)
packages/i18n/src/catalogs/{vi,en}.ts        (đụng — CHỈ trong block `admin:` hiện có (cuối file) — ANCHOR P1: SF-3 chèn checkout:/shell: sau auth:, SF-4 chèn account: sau nav: — khác anchor = merge sạch; thêm key admin.* mới nếu thiếu, additive)
```
READ-ONLY: `packages/ui-kit/**` (SF-1 own — bug → flag), `apps/shell/**` (SF-3), `apps/mfe-account/**` (SF-4), `apps/storefront-web/**` (SF-2), `packages/i18n` namespace khác (checkout.*/account.* — KHÔNG đụng), `contracts/**`, `backend/**`, e2e/ (read-only, chạy subset). pnpm-lock dep freeze.

## ACCEPTANCE (user-visible)

1. Login admin → sidebar có icon + active state + group rõ; đi hết 14 màn visual đồng bộ một ngôn ngữ design.
2. Bấm header cột bảng (products/orders/coupons...) → sort asc/desc client-side ngay, aria đúng; chọn page-size 10/25/50 → số dòng đổi; header bảng sticky khi cuộn; loading là skeleton.
3. Pagination nhìn chuyên nghiệp (số trang + prev/next) thay "← Trước/Sau".
4. Product form: grid thoáng, upload area trực quan, variants grid dễ đọc — CRUD vẫn hoạt động đúng (e2e admin-crud chứng minh).
5. Dark mode admin: 4 trạng thái theme sạch, không hex lệch, `admin-badge-mock` không còn.
6. e2e subset (admin-coupon, admin-crud, upload-image, rbac) XANH + unit mfe-admin xanh.

## Boundary (KHÔNG làm)

**E2E port base: +300** (mock-gateway :9099+300 + alt-ports — SF-2 +0, SF-3 +100, SF-4 +200).

- KHÔNG sửa `packages/ui-kit/**` (bug → flag); KHÔNG server-side pagination/sort/filter (cần backend — Q5), KHÔNG bulk action.
- KHÔNG đụng react-query logic/state-machine guard/RBAC boot/CSV/invoice logic — chỉ presentation.
- KHÔNG key i18n ngoài `admin.*`; KHÔNG đụng catalogs namespace checkout.*/account.*; KHÔNG FULL e2e (SF-6); KHÔNG merge vào main.
