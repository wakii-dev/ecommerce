# SF-1 Context Pack — chrome-package

> Đọc file này THAY VỀ tự tổng hợp. Epic spec: `docs/superpowers/specs/2026-09-08-frontend-unification-design.md` · Bracket: `docs/superpowers/brackets/fi397-unify-frontend.md` · Linear: FI-397 (epic) · Nhánh đích: `story/fi397-unify-frontend` (fork TỪ story/fi390-uiux-elevation sau khi FI-390 Done).
> **PRECONDITION:** FI-390 ĐÃ merge — Header/Footer 2 app đang ở trạng thái elevated (direction FI-390). Chrome phải GIỮ visual FI-390 (wrap, không redesign). `contracts/**` + `backend/**` READ-ONLY. Zero backend change. Dep freeze.

## Spec slice (chỉ phần SF-1 chịu trách nhiệm)

1. **Scaffold `frontend/packages/chrome`**: package.json (exports map, deps: ui-kit + auth + i18n), tsconfig; **TS source thuần** — Next consume qua `transpilePackages` (thêm 1 dòng vào next.config.mjs:5), Vite consume qua workspace dep; KHÔNG dựng rollup/dual-output. Đăng ký turbo.json + pnpm workspace nếu cần.
2. **`chrome-vite-preset-shared-singletons`**: thêm `@ecommerce/chrome` vào `SHARED_SINGLETONS` (`packages/config/vite-preset.mjs:19-30`) — ĐÚNG 1 dòng (singleton:true, requiredVersion:false). LÝ DO P0: không có singleton, shell host + mfe-checkout remote bundle 2 instance chrome → registry tách 2 → badge đăng ký từ remote biến mất trên host render (đúng lớp bug i18next BUG-04). **Gate: chứng minh badge đăng ký từ mfe-checkout hiện trên shell host render.**
3. **Registry move**: chuyển `HeaderSlots` registry (`apps/shell/src/header/HeaderSlots.ts` — Map SlotKey×id, API register/unregister/list, CustomEvent 'ecommerce:header-slots-changed') vào chrome package. **SF-1 sở hữu TẤT CẢ import call-site updates**: shell `main.tsx:27,29` (ShellNav + ThemeToggle), `mfe-account/src/bootstrap.tsx:43-47` (auth+orders+affiliate), `mfe-checkout/src/bootstrap.tsx:52` (cart-badge) đổi import sang chrome. Shell giữ CustomEvent wiring (App.tsx:147-151) hoặc chrome expose helper — không đổi behavior.
4. **Shell Header adapter (final state SF-1)**: `apps/shell/src/header/Header.tsx` thành adapter mỏng render chrome Header + feed registry; visual GIỮ nguyên trạng FI-390 elevated. SF-4 sẽ không đụng file này — bạn sở hữu final state.
5. **SiteHeader SSR-able**: component chính nhận slots qua props/registry, render server được trong Next RSC (links trong HTML đầu — SEO + nav-honesty); islands (cart-badge, auth-menu, theme-toggle, locale-switcher) là `'use client'`. Nav labels/quy tắc layout theo direction FI-390 (docs/superpowers/designs/fi390-uiux-elevation-direction.md).
6. **Footer port**: port Footer vào chrome (server-safe), Next + shell cùng dùng.
7. **ThemeToggle canonical**: key **`ecommerce.theme`** (shell ThemeToggle.tsx:16 đang dùng); boot script anti-FOUC export từ chrome (1 nguồn); nếu key khác tồn tại (FI-390 storefront) → read-order migration (key khác = fallback cũ, lần toggle đầu ghi về canonical). storefront-web hiện 0 hit theme code — nếu FI-390 thêm toggle ở header storefront, chrome ThemeToggle là nguồn duy nhất (SF-4 sẽ swap).
8. **CartBadge extract**: chuyển logic badge từ `mfe-checkout/src/CartBadge.tsx` sang chrome (đếm từ cartApi GET + subscribe `ecommerce:cart-changed` window event + `authStore.subscribe` — GIỮ nguyên 3 nguồn này, KHÔNG tạo state cart thứ 2); mfe-checkout CartBadge thành wrapper import chrome (giữ đăng ký `'right','checkout-cart-badge'` nguyên trạng).
9. **AuthMenu extract**: chuyển UserMenu dropdown từ `mfe-account/src/AuthWidget.tsx` sang chrome (authStore, keyboard menu — FI-390 đã nâng cấp); account AuthWidget thành wrapper giữ đăng ký 3 slot.
10. **SessionBoot provider**: client provider gọi ensureSession pattern hiện có (storefront `lib/account-session.ts:18-29` — configureAuth + single-flight refresh) — chrome export để cả 2 host dùng; KHÔNG đụng sync (SF-2).
11. **i18n contract**: chrome KHÔNG import i18next trực tiếp — nhận `t` qua provider/props từ host (I18nextProvider pattern AdminApp.tsx:240); labels bind namespace **`chrome.*`** additive vào `packages/i18n/src/catalogs/{vi,en}.ts` (CHỈ namespace chrome.*, vi+en song song). CẤM props-label per-element (phá "sửa 1 chỗ").
12. **LocaleSwitcher** vào chrome (client island).
13. **Unit tests**: vitest jsdom + SSR renderToStaticMarkup 2 chế độ (server pass + client pass) + single-instance test (2 consumer cùng module instance).
14. **/ui-kit demo showcase**: thêm chrome showcase vào `apps/shell/src/pages/UiKitDemoPage.tsx` (compose thủ công — nhớ thêm) với badge-from-remote evidence.

## Touch map (files SF-1 tạo/sở hữu)

```
frontend/packages/chrome/**                          (sở hữu — package mới)
frontend/packages/config/vite-preset.mjs             (đụng 1 dòng SHARED_SINGLETONS — anchor: chỉ dòng này)
frontend/packages/i18n/src/catalogs/{vi,en}.ts       (đụng — CHỈ namespace chrome.* additive)
frontend/apps/shell/src/header/Header.tsx            (sở hữu FINAL — adapter)
frontend/apps/shell/src/header/HeaderSlots.ts        (chuyển sang chrome — shell import lại)
frontend/apps/shell/src/main.tsx:27,29               (đụng — đổi import chrome)
frontend/apps/shell/src/pages/UiKitDemoPage.tsx      (đụng — showcase)
frontend/apps/mfe-checkout/src/bootstrap.tsx:52 + CartBadge.tsx   (đụng — import chrome + wrapper)
frontend/apps/mfe-account/src/bootstrap.tsx:43-47 + AuthWidget.tsx (đụng — import chrome + wrapper)
frontend/apps/storefront-web/next.config.mjs         (đụng 1 dòng transpilePackages — anchor)
```
READ-ONLY: `packages/auth/**` (SF-2 own), `packages/ui-kit/**` (FI-390 — consume thôi, bug → flag), `apps/storefront-web/app/**` layout (SF-4 swap), `apps/mfe-{checkout,account}/src/pages/**` (không đụng — chỉ bootstrap imports + 2 widget files), `contracts/**`, `backend/**`, e2e/, pnpm-lock (0 dep mới). CẢ NH VỠ: FI-390 vừa polished các file này — giữ visual, đổi cấu trúc tối thiểu.

## ACCEPTANCE (user-visible)

*(P0 re-scope: demo Next-layout render + cross-app label/theme sync thuộc SF-4 — cần layout swap + 1-origin; gate của bạn chỉ những gì SF-1 tự cung cấp.)*

1. Shell render Header/Footer từ chrome (adapter) — visual GIỮ nguyên FI-390 direction (4 trạng thái theme, dev servers offset +500: shell 5673, remotes 5675-77, Next 3500 qua REMOTE_*_URL/PORT env).
2. Click giỏ trên SHELL: badge từ cart-badge component chrome (đăng ký bởi mfe-checkout) hiện đúng số — chứng minh host+remote dùng 1 instance chrome (không 2 registry).
3. `renderToStaticMarkup(<SiteHeader/>)` + `(<Footer/>)` sạch server-side (unit evidence — Next SSR-ready cho SF-4 wire sau).
4. Theme toggle trong chrome ghi đúng key `ecommerce.theme`; boot script export hoạt động (unit + manual check).
5. `pnpm vitest run` packages/chrome + i18n xanh (unit + SSR 2 chế độ + single-instance test); /ui-kit demo có chrome showcase.

## Boundary (KHÔNG làm)

**Dev ports (P0):** chạy dev servers offset +500 — shell `--port 5673`, remotes 5675/5676/5677 (`REMOTE_*_URL` env trỏ đúng), storefront 3500. Cấm giữ :3000/:5173 (SF-3 probe +600 và FI-390 worktrees đang dùng).
- KHÔNG đụng `packages/auth/**` (SF-2 — kể cả session-sync; SessionBoot của bạn chỉ ensureSession hiện có).
- KHÔNG đụng dev entry/proxy (SF-3), Next layout swap (SF-4), scripts/.env (SF-3).
- KHÔNG đụng `apps/mfe-{checkout,account}/src/pages/**` logic (chỉ bootstrap import + 2 widget wrapper); KHÔNG đổi registration flow HeaderSlots.
- KHÔNG đụng backend/gateway/nginx/docker-compose; KHÔNG thêm dependency mới; KHÔNG merge vào main.
- Gate KHÔNG assert hiệu ứng session-sync (SF-2) hay 1-URL entry (SF-3) — chỉ những gì SF-1 cung cấp.
