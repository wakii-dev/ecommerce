# SF-3 Context Pack — dev-one-origin-entry

> Đọc file này THAY VỀ tự tổng hợp. Epic spec: `docs/superpowers/specs/2026-09-08-frontend-unification-design.md` · Bracket: `docs/superpowers/brackets/fi397-unify-frontend.md` · Linear: FI-397 (epic) · Nhánh đích: `story/fi397-unify-frontend`.
> **PRECONDITION:** FI-390 ĐÃ merge. Prod KHÔNG đổi (đã 1 entry gateway :8080 — nginx shell / + remotes /remotes/<name>/, gateway-routes.yml:55-61 + :116-118). Zero backend change. Dep freeze (Vite http-proxy + Next rewrites có sẵn).

## Spec slice (chỉ phần SF-3 chịu trách nhiệm)

0. **User chốt (09-09): entry = `http://localhost:3000`** — bao gồm `/admin` (rewrite → shell, AdminApp render layout RIÊNG full-bleed KHÔNG chrome wrap — admin chỉ cần ĐẾN ĐƯỢC, không đụng gì khác).
1. **PROBE (task đầu — evidence bắt buộc)**: entry :3000 qua Next rewrites + **HMR workaround**: mỗi Vite server set `server.hmr.clientPort` (ws nối TRỰC TIẾP tới port riêng, bypass entry origin — Vite hỗ trợ đúng scenario reverse-proxy); remoteEntry URLs remotes phải same-origin qua entry (rewrite `/remotes/**` → server tương ứng hoặc base tương đối). Fallback nếu HMR vẫn chết → Vite-proxy/5174 ghi ADR (entry cuối vẫn phải :3000 — front-router).
2. **Implement entry** (config-only): `next.config.mjs` rewrites() ĐÃ TỒN TẠI (/api, /media) — **APPEND shell-routes vào array, KHÔNG thay thế function** (P2): `/cart|/checkout|/order/confirmation|/account|/login|/register|/forgot-password|/reset-password|/admin|/admin/**|/remotes/**|/assets/**|/@vite/**|/src/**|/node_modules/**` → `http://localhost:5173`; đảm bảo cookie/origin nhất quán (1 origin → BroadcastChannel SF-2 hoạt động dev; guest cart localStorage key chung origin).
3. **dev-stack** (`scripts/dev-stack.sh:165-180`): guard curl entry-URL mới, banner ports cập nhật, health check entry; **dev-stop** (`scripts/dev-stop.sh` port list 3000/5173-5178) giữ đủ.
4. **env updates**: `.env.example:58-63` REMOTE_*_URL + SITE_URL/NEXT_PUBLIC_SHELL_URL defaults theo entry; `lib/site.ts:16-18` `shellUrl()` → same-origin relative `''` (link `shellUrl()+'/cart'` ra `/cart` — cấm `//cart`; localePath vi-no-prefix memory); dev-only fallback qua env nếu entry chưa bật.
5. **e2e helpers** (`frontend/e2e/helpers/env.ts:37-42`): STOREFRONT/SHELL defaults → cùng entry URL (specs đọc env — đổi defaults là đủ cho đa số; auth-cookie.spec :21 HTTP-level qua proxy re-verify qua entry mới).
6. **Docs**: README/docs dev entry 1 URL + cách tắt (env flag fallback 2-origin nếu ai cần debug riêng lẻ — optional note).
7. **Probe evidence + golden path 1-URL**: walkthrough home→PDP→cart→checkout→confirmation→account qua ĐÚNG 1 URL không đổi port; screenshots.
8. **KHÔNG đụng**: `apps/storefront-web/middleware.ts` (SF-2 sở hữu redirect guard; locale rewrite + affiliate ?ref giữ nguyên), chrome package (SF-1), packages/auth (SF-2). `next.config.mjs` chỉ rewrites block (anchor — transpilePackages là vùng SF-1); `vite-preset.mjs` nếu cần — KHÔNG đụng SHARED_SINGLETONS + plugin redirect (vùng SF-1/SF-2).

## Touch map (files SF-3 tạo/sở hữu)

```
frontend/apps/shell/vite.config.ts hoặc storefront next.config.mjs (sở hữu — entry proxy/rewrites theo probe; anchor vùng riêng)
scripts/dev-stack.sh + scripts/dev-stop.sh            (sở hữu — entry guard/banner/port list)
frontend/.env.example                                  (đụng — REMOTE_*/SITE_URL/NEXT_PUBLIC_SHELL_URL defaults)
frontend/apps/storefront-web/lib/site.ts              (đụng — shellUrl() same-origin relative)
frontend/e2e/helpers/env.ts                           (đụng — STOREFRONT/SHELL defaults)
docs/ (adr probe + README dev entry)                  (sở hữu)
```
READ-ONLY: `packages/{chrome,auth}/**` (SF-1/2), `apps/*/src/**` runtime code, gateway/nginx/docker-compose (prod không đổi — gateway-routes.yml:113-118 comment dev vào :5173 trực tiếp: cập nhật comment nếu cần, KHÔNG đổi predicate), `contracts/**`, `backend/**`, pnpm-lock. LƯU Ý: dev-stack là script chung — đừng phá `make dev` cho người không dùng entry mới (env flag opt-in hoặc default-on nhất quán, ghi ADR).

## ACCEPTANCE (user-visible)

1. Dev mở ĐÚNG `http://localhost:3000`: đi hết home→PLP→PDP→cart→checkout→confirmation→account KHÔNG đổi port tay — mọi trang render đúng + actions hoạt động (add-to-cart, login, checkout guest).
2. `http://localhost:3000/admin` mở được AdminApp với LAYOUT RIÊNG (sidebar/topbar admin, không chrome Header/Footer) — admin không bị ảnh hưởng.
3. HMR sống cả 2 phía: sửa 1 file Next component → hot update trang Next; sửa 1 file remote → hot update trang shell (probe evidence video/screenshot + log — clientPort ws trực tiếp).
4. remoteEntry.js của 3 remotes load được qua entry (network tab evidence — không CORS/404).
5. `shellUrl()`-based links (header cart/account, buy-now) ra URL same-origin đúng `/cart` (không `//cart`, không absolute cross-port).
6. e2e subset (golden-path, nav-honesty) XANH với env defaults mới; auth-cookie.spec vẫn xanh qua entry proxy.

**Dev ports (P0):** PROBE dùng offset +600 (Next 3600, shell 5773, remotes 5775-77 qua env) — cấm giữ :3000/:5173 khi SF-1/SF-2 đang chạy; entry CUỐI CÙNG bind :3000 sau probe (thông báo coordinator khi chuyển — :3000 là URL chính thức).

## Boundary (KHÔNG làm)

- KHÔNG đụng `packages/chrome/**`, `packages/auth/**` (SF-1/2), `apps/storefront-web/middleware.ts` (SF-2), layout swap (SF-4).
- KHÔNG đổi gateway routes/nginx/docker-compose (prod đã 1 entry — chỉ verify ở SF-5); KHÔNG đổi runtime app code (config-only).
- KHÔNG tắt 2-origin hoàn toàn nếu phá workflow ai đó — env fallback ghi ADR; KHÔNG thêm dependency; KHÔNG merge vào main.
- Gate KHÔNG assert session-sync tức thì (SF-2) hay chrome rendering (SF-1/4) — chỉ entry/HMR/remote/golden-path.
