# SF-4 Context Pack — consume-chrome-wiring

> Đọc file này THAY VỀ tự tổng hợp. Epic spec: `docs/superpowers/specs/2026-09-08-frontend-unification-design.md` · Bracket: `docs/superpowers/brackets/fi397-unify-frontend.md` · Linear: FI-397 (epic) · Nhánh đích: `story/fi397-unify-frontend`.
> **PRECONDITION:** SF-1 + SF-3 ĐÃ merge (chrome package + registry move + shell adapter final state SẴN SÀNG — KHÔNG viết lại adapter đó; entry 1-origin :3000 sống — demo cross-app của bạn chạy trên đó; việc của bạn = HOÁN ĐỔI COMPONENT đăng ký + wire Next side). `contracts/**` + `backend/**` READ-ONLY. Dep freeze.

## Spec slice (chỉ phần SF-4 chịu trách nhiệm)

1. **Next layout swap**: `apps/storefront-web/app/[locale]/layout.tsx` — Header/Footer local → chrome components (SiteHeader server-safe + islands client); đăng ký/wire slots cho Next (nav links locale-aware qua localePath — nhớ vi-no-prefix memory); SessionBoot provider từ chrome bọc client tree.
2. **Delete dup storefront**: xóa `components/Header.tsx` + `components/Footer.tsx` markup riêng sau khi chrome thay thế (xóa code là PHẦN CỦA task — không để dead code); islands local vẫn cần (SearchBar combobox, WishlistHeart...) — chỉ chrome-able parts swap, KHÔNG đụng logic islands kia.
3. **Theme canonical adopt**: bỏ mọi theme toggle local storefront (nếu FI-390 đã thêm) → chrome ThemeToggle; key `ecommerce.theme` chuẩn; boot script anti-FOUC từ chrome (1 nguồn).
4. **GA/livechat wiring qua chrome boot**: `components/GaPageview.tsx` + `components/LiveChat.tsx` storefront → wire qua chrome boot exports (contract `window.gtag` + purchase event checkout + livechat double-inject guard GIỮ NGUYÊN hành vi).
5. **Cart-badge/account-menu component swap (exit criteria CỨNG — P1)**: `mfe-checkout/src/bootstrap.tsx` + `mfe-account/src/bootstrap.tsx` đăng ký component TỪ `@ecommerce/chrome` TRỰC TIẾP; wrapper CartBadge/AuthWidget nội bộ (nếu SF-1 để lại) XÓA; grep 0 import wrapper sót; registration flow HeaderSlots KHÔNG đổi. **Admin KHÔNG đụng** (không chrome wrap — chỉ smoke /admin vào được qua entry).
6. **i18n wire host-provider**: Next bọc I18nextProvider (initI18n + namespace chrome.*) — storefront KHÔNG dùng @ecommerce/i18n trước giờ: đây là lần đầu wire (pattern AdminApp.tsx:240); các COPY module storefront hiện có GIỮ NGUYÊN (SF-390 đã gom — chỉ chrome labels đi qua t-provider mới).
7. **Unit tests update**: storefront tests + shell tests vỡ do swap → fix xanh; thêm test nhỏ assert layout render chrome (markup chứa chrome class/registry feed).
8. **e2e subset**: golden-path + nav-honesty XANH (nav-honesty regex link `/cart|/account` relative giữ pass — chrome Header phải render links tương tự); chạy sequential per SF-1 QA recipe nếu stack chung.
9. **Walkthrough cross-app nav**: storefront → cart → account → storefront screenshots; theme + locale đồng bộ 2 app.
10. **Xóa NEXT_PUBLIC_SHELL_URL cross-origin uses**: sau shellUrl() same-origin (SF-3) — grep + dọn uses đã chết (docker-compose env :502 giữ nguyên — prod vẫn cần; chỉ dọn code path dev chết).

## Touch map (files SF-4 tạo/sở hữu)

```
frontend/apps/storefront-web/app/[locale]/layout.tsx   (sở hữu — swap chrome + providers)
frontend/apps/storefront-web/components/Header.tsx     (sở hữu — XÓA sau swap)
frontend/apps/storefront-web/components/Footer.tsx     (sở hữu — XÓA sau swap)
frontend/apps/storefront-web/components/{ThemeToggle,GaPageview,LiveChat}.tsx (sở hữu — swap/đổi nguồn chrome)
frontend/apps/mfe-checkout/src/bootstrap.tsx           (đụng — component swap đăng ký)
frontend/apps/mfe-account/src/bootstrap.tsx            (đụng — component swap đăng ký)
frontend/apps/storefront-web/tests/** + apps/shell tests (đụng — fix xanh)
```
READ-ONLY: `packages/chrome/**` (SF-1 — bug → flag coordinator), `packages/auth/**` (SF-2), shell Header adapter + HeaderSlots (SF-1 final state — KHÔNG viết lại), entry/proxy configs (SF-3), `contracts/**`, `backend/**`, pnpm-lock. LƯU Ý: FI-390 vừa elevated Header/Footer/islands — swap giữ nguyên VISUAL (chrome đã theo direction FI-390); đổi cấu trúc tối thiểu.

## ACCEPTANCE (user-visible)

1. Storefront Header/Footer render từ chrome — nhìn giống hệt FI-390 direction; markup header riêng cũ KHÔNG còn trong repo (grep evidence).
2. Sửa 1 nhãn trong keys `chrome.*` → cả Next + shell hiện nhãn mới, không sửa app nào (evidence walkthrough).
3. Theme toggle 1 nguồn: toggle ở storefront → shell đồng bộ (cùng key); GA pageview + livechat vẫn bắn đúng (network evidence); cart-badge + account menu hoạt động cả 2 app.
4. Cross-app nav mượt: storefront → cart → account → back; locale switch giữ path (vi không prefix); nav-honesty + golden-path e2e XANH.
5. Unit tests storefront + shell xanh sau swap.

## Boundary (KHÔNG làm)

- KHÔNG đụng `packages/chrome/**` (SF-1 — thiếu gì → flag coordinator, KHÔNG tự sửa chrome), `packages/auth/**` (SF-2), entry/proxy (SF-3).
- KHÔNG viết lại shell Header adapter/HeaderSlots (SF-1 final state); KHÔNG đổi registration flow (chỉ component nguồn).
- KHÔNG đụng islands logic (SearchBar/WishlistHeart/reviews... chỉ chrome-able parts swap); KHÔNG đụng docker-compose env prod (:502 giữ); KHÔNG thêm dep; KHÔNG merge vào main.
- Gate KHÔNG assert sync tức thì (SF-2) hay entry HMR (SF-3) — chỉ chrome rendering + swap + tests.
