# SF-5 Context Pack — convergence-qa

> Đọc file này THAY VỀ tự tổng hợp. Epic spec: `docs/superpowers/specs/2026-09-08-frontend-unification-design.md` · Bracket: `docs/superpowers/brackets/fi397-unify-frontend.md` · Linear: FI-397 (epic) · Nhánh đích: `story/fi397-unify-frontend`.
> SF-1..4 ĐÃ merge về nhánh đích — bạn verify TRÊN code hợp nhất. **Vai: QA độc lập — KHÔNG tự sửa code surface.** Sweep fail → sinh fix task gán SF sở hữu (SF-1 chrome / SF-2 auth / SF-3 entry / SF-4 wiring) → re-run cap 2 vòng. `contracts/**` + `backend/**` READ-ONLY.

## Spec slice (chỉ phần SF-5 chịu trách nhiệm)

1. **Sync matrix (2 pages CÙNG Playwright context × 2 app — P1: multi-context = cookie jar + storage riêng → BC/cookie không xuyên, sai primitive)**: login A → B thấy user (≤1 POST refresh/page qua network log, KHÔNG reload — assert không navigate); logout A → B out; 2FA challenge không broadcast session sai; **20 chạy liên tiếp rotate-race = 0 spurious logout**; OAuth callback broadcast đúng. Docker full isolated +400 per recipe (xem pack SF-2 — same recipe, project name fi397sf5).
2. **One-origin golden path**: walkthrough qua entry-URL (SF-3 chọn — đọc ADR probe): home→PLP→PDP→cart→checkout(guest+COD hoặc Stripe test)→confirmation→account không đổi port; HMR evidence từ SF-3 giữ nguyên trạng thái (re-verify 1 lần).
3. **127-redirect matrix**: `curl -I 127.0.0.1:{3000,5173}` → redirect localhost giữ path+query; `/_next/*` + static assets KHÔNG bị redirect; localhost trực tiếp không loop.
4. **e2e FULL 14 specs**: cả suite (`frontend/e2e/tests/*.spec.ts`) trên stack nhánh đích — alt-ports **+400** (FI-390 dùng +0..+300 từ mock-gateway :9099), compose project-name riêng nếu cần full stack (container global — tránh đụng FI-390 worktrees); selector vỡ → fix trong specs (SF-5 own e2e) hoặc fix-task về SF nếu code surface đổi classname.
5. **Docker full regression (isolated +400)**: `make full`-tương đương isolated — :8080+offset toàn flow OK; **gateway-routes.yml + nginx frontend-web.conf KHÔNG đổi** (diff check); storefront-web/nginx path layout nguyên trạng.
6. **Chrome cross-host visual consistency + theme legacy-key migration check (P2)**: Header/Footer/badge/menu/theme 4 trạng thái × 2 host — identical; single-instance evidence (badge remote-registration) còn đúng; **legacy theme key migration**: seed key cũ (nếu FI-390 giới thiệu key khác `ecommerce.theme`) → toggle 1 lần → giá trị ghi về canonical (read-order migration assert).
7. **Unit tests monorepo**: chrome + auth + i18n + 5 apps — toàn bộ xanh trên nhánh đích.
8. **Perf sanity (binary gate)**: chrome KHÔNG bị double-inclusion trong 1 bundle — build Next + shell, grep bundle cho duplicate chrome module markers (vd 2 bản chrome registry class); fail = fix-task SF-1 (singleton config) ; KHÔNG đo CLS/MS thừa — chỉ check này.
9. **Sweep §7.9/§7.10**: `git diff main-fork-point..HEAD -- pnpm-lock.yaml` rỗng (0 dep mới); `git diff -- backend/ contracts/` rỗng (zero backend change).
10. **Walkthrough record + ADR roadmap**: record video/screens toàn surfaces (Orca browser tab cho user); viết `docs/adr/` roadmap migration (b) — thứ tự account→checkout, 7 blockers (appNavigate injection, authReady timing, singleton instance per origin, page.css, gateway/nginx routing, e2e re-verify, GA/theme boot contracts), exit criteria từng giai đoạn.

## Touch map (files SF-5 tạo/sở hữu)

```
frontend/e2e/**                               (sở hữu — fix selector trong specs, thêm sync-matrix spec)
docs/superpowers/qa/* + docs/adr/*            (sở hữu — sweep reports + roadmap ADR)
scripts/* (nếu cần probe script)              (sở hữu)
```
ĐỌC-ONLY toàn bộ app/package code. Fix code KHÔNG phải việc bạn (trừ e2e specs) — sinh fix task + comment epic gán SF sở hữu; re-run sau fix merge.

## ACCEPTANCE (user-visible)

1. Sync demo video: login/logout lan tức thì 2 tab 2 app, không reload, không spurious logout (20-run evidence).
2. 1 URL dev đi hết golden path mua hàng (entry SF-3) — recordings.
3. e2e FULL 14 specs XANH (1 lệnh, evidence epic comment); unit tests monorepo xanh.
4. Docker full regression + gateway/nginx diff sạch; 127-redirect matrix đúng; chrome không double-bundle (build evidence).
5. ADR roadmap migration (b) có trên repo; walkthrough record user xem được — chuẩn bị final verify STORY-COMPLETE.

## Boundary (KHÔNG làm)

- KHÔNG sửa code app/package (trừ e2e specs + scripts QA) — fix surface → fix task về SF sở hữu.
- KHÔNG thêm feature mới; KHÔNG đụng gateway/nginx/docker-compose (chỉ diff-check); KHÔNG backend.
- KHÔNG re-design chrome (đã duyệt qua FI-390 direction) — verify là chính; KHÔNG merge vào main (PR là quyền người).
