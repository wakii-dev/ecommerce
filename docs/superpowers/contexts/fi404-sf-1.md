# SF-1 Context Pack — qa-static-audit

> Đọc file này THAY VỀ tự tổng hợp. Epic spec: `docs/superpowers/specs/2026-09-10-qa-sweep-design.md` · Bracket: `docs/superpowers/brackets/fi404-qa-sweep.md` · Linear: FI-404 (epic) · Nhánh đích: `story/fi404-qa-sweep` (fork từ master @846648d+).
> Scripts đặt ở `scripts/qa/` (10 scripts cùng kiểu đã có làm precedent). Dep freeze. KHÔNG đụng service code (fix = SF-4).

## Spec slice (chỉ phần SF-1 chịu trách nhiệm)

1. **config-audit trục (a) env vars**: extract mọi `${VAR:default}` từ `backend/**/application*.yml` + `@Value("${...}")` annotations + `backend/gateway/src/main/resources/gateway-routes.yml` (URI defaults) → so với `environment:` blocks trong `docker-compose.yml`. Classify machine-checkable: **DANGEROUS** = default match `localhost|127.0.0.1` + consumer service có compose entry + compose không set VAR đó (pattern LOG_URI); WARN = compose set nhưng default khác value; OK = set khớp. Whitelist FE-host: storefront/shell/FE dev chạy host là chủ đích (default localhost:3000/5173 trong FE code không flag).
2. **Trục (b) volume mounts drift**: với mỗi service container, collect file/path code đọc lúc boot (grep `JwtDecoderConfig`, PEM/keystore paths, `@Value` path vars) ↔ compose `volumes:` mount — thiếu mount/path lệch = DANGEROUS (pattern JWT keys: `JWT_PUBLIC_KEY_PATH=/keys/jwt-public.pem` cần `./infra/keys:/keys:ro`).
3. **Trục (c) compose value drift**: settings quan trọng trong compose so giá trị hợp lý — `command:` flags (max_connections!), healthcheck timeouts, resource limits; flag khi default thấp hơn nhu cầu đã biết (11 JVM pools).
4. **s2s-auth-matrix STATIC** (KHÔNG HTTP call): enumerate MỌI HTTP client service→service (grep RestClient/WebClient/RestTemplate/`http://` service-name refs): ordering 6, cart 2, catalog 1, notification 3, partner 3... Với từng pair: URL path gọi (resolving env/path property) → endpoint cần auth gì (đọc SecurityConfig service đích: permitAll/admin/authenticated) → client có gắn token không → verdict EXPECTED OK/DANGEROUS/GAP. Bảng markdown.
5. **RBAC expected matrix**: guest/user/admin × MỌI admin endpoints (đóng list: catalog admin products/categories, ordering admin coupons, inventory admin stocks+low-stock, log admin events, partner admin, RMA admin, newsletter, audit) + public paths điểm danh. Verdict EXPECTED per cell.
6. **Report**: `docs/superpowers/qa/report-sf1.md` — classify + matrix + findings list; **exit-code semantics**: 0 = 0 finding CHƯA fix; 1 = có finding chưa fix; 2 = script error. Finding FIXED ghi status FIXED (không ảnh hưởng exit).
7. **makefile target `qa-audit`**: chạy 2 script + exit code propagate.
8. **Makefile + docs**: target comments + README QA section ngắn.
9. **unit smoke + self-test fixture negative**: script có `--self-test` chạy trên FIXTURE compose thiếu env (file fixture, KHÔNG tạm revert compose thật — read-only) → DANGEROUS bắt được.
10. **probe contracts freshness**: diff `contracts/` generated client ops vs controllers thực tế — stale → finding riêng (không tự sửa).
11. **epic comment findings P0**: gộp vào task report — đăng report tóm tắt lên FI-404 (`orca linear comment add --id FI-404 --body-file -`).

## Touch map (files SF-1 tạo/sở hữu)

```
scripts/qa/config-audit.mjs (hoặc .sh)      (sở hữu)
scripts/qa/s2s-auth-matrix.mjs (hoặc .sh)   (sở hữu)
scripts/qa/rbac-matrix.*                    (sở hữu)
docs/superpowers/qa/report-sf1.md           (sở hữu)
Makefile                                    (đụng — target qa-audit; cộng tác)
docs/superpowers/qa/report-sf2.md           (KHÔNG đụng — SF-2)
docs/superpowers/qa/bug-register.md         (KHÔNG tạo — SF-4 own + merge)
```
READ-ONLY: mọi service code (chỉ ĐỌC để extract env/auth), docker-compose.yml (chỉ đọc — fix env = SF-4 qua finding), contracts/ (đọc), backend/gateway gateway-routes.yml (đọc). Dep freeze. KHÔNG chạy fresh-boot (SF-2). KHÔNG HTTP probe runtime (static only — live RBAC là harness/SF-4).

## ACCEPTANCE (user-visible)

1. `make qa-audit` < 5 phút, exit semantics đúng; report `report-sf1.md` classify 3 trục + 2 matrix đóng.
2. Detector phủ đủ bug thật: fixture compose thiếu env → DANGEROUS bắt được (fixture-only — KHÔNG tạm revert compose thật, read-only); compose hiện tại (đã fix) → status OK cho LOG_URI/keys/max_connections.
3. s2s matrix ≥ 23 pair, từng pair verdict có理由 (file:line client + SecurityConfig đích).
4. RBAC expected matrix đóng (list endpoint đủ theo §2.8 spec).
5. `--self-test` fixture pass; `make qa-audit` không đụng docker runtime (non-destructive chứng minh bằng code đọc).

## Boundary (KHÔNG làm)

- KHÔNG chạy HTTP probe/runtime (harness stage-6 + SF-4 mới execute live); KHÔNG đụng service code/compose (fix = SF-4 qua finding); KHÔNG tạo bug-register.md (SF-4).
- KHÔNG đụng scripts/dev-stack.sh; KHÔNG fresh-boot; KHÔNG thêm dependency; KHÔNG merge vào main.
- Gate KHÔNG assert harness/journeys (SF-2/3) — chỉ detector tĩnh.

## Amendment 1 — 2026-09-10 (spec-critic round 1, run FI-405)

> Text gốc giữ nguyên phía trên. Amendment giải quyết 2 P0 + 7 P1 của spec-critic —
> không đổi scope (5 lớp bug, static-only). Epic FI-404 được thông báo theo format
> REQUIREMENT-GAP với default khai báo; veto → SF-1 rework TRƯỚC merge (rework cap 2).

- **A1 — Classify trục (a) machine-checkable:** WARN **không phải finding**, không ảnh hưởng exit (finding = DANGEROUS + GAP; nếu WARN là finding thì compose healthy exit 1 vĩnh viễn — trái exit semantics "0 = 0 finding CHƯA fix"). **OK gồm alias-equivalence**: compose set cùng **port** với default + host là compose service name (hoặc trùng default/localhost-family) — vd default `http://localhost:8088` ↔ compose `http://log-service:8088` = OK; đây đúng hình thái "đã fix" của LOG_URI 9/9 (ACCEPTANCE #2 yêu cầu status OK). WARN = compose set nhưng lệch ngữ nghĩa (khác port/path/host lạ). **FIXED registry** = file checked-in `scripts/qa/config-audit-fixed.json` (finding ID + evidence + date); script đọc mỗi run, ID khớp → status FIXED, không ảnh hưởng exit.
- **A2 — s2s pair := (client class, endpoint path gọi)** — KHÔNG phải service-pair (service-pair thật chỉ ~15 < 23). Path-granularity đạt ≥23 tự nhiên (vd ordering InventoryClient reserve/confirm/release = 3 pair). ACCEPTANCE #3 "≥ 23 pair" đọc theo định nghĩa này.
- **A3 — s2s enumeration loại trừ:** chỉ pair có base-url resolve vào compose service (map module→compose name là constant trong script); loại infra (ES/MinIO/SMTP/Rabbit) + third-party (GHN/Stripe/OAuth IdP/partner webhooks/host.docker.internal/stripe-cli).
- **A4 — Trục (a) file scope:** MỌI `backend/gateway/src/main/resources/routes/*.yml` (gồm `gateway-auth.yml` — JWKS URI default), không chỉ `gateway-routes.yml`.
- **A5 — Verdict taxonomy s2s:** EXPECTED_OK (path permitAll, hoặc client gắn token đủ role cho matcher đích) / DANGEROUS (path cần auth, client không gắn token) / GAP (không resolve tĩnh được — liệt kê lý do; **GAP = finding → exit 1**; SF-4 triage về 0). Epic §5.2 "0 pair UNKNOWN" ≡ 0 GAP.
- **A6 — Trục (b) rule:** env path (code default hoặc compose env) của service phải nằm dưới (prefix-match) 1 volume container-target của chính service đó. Code cần path mà không có env+volume → DANGEROUS; volume có nhưng path không thuộc target → DANGEROUS (path lệch); host-relative default (`../infra/keys`) chuẩn hóa theo basename trước match. Tiers như trục (a) (WARN không phải finding).
- **A7 — Trục (c) closed checklist (không judgment runtime):** (1) postgres `command:` `max_connections` ≥ 110 (11 JVM pools × 10 Hikari default; hiện 300 = OK); (2) mỗi JVM service có compose entry phải có `healthcheck:`; (3) flag khác ngoài bảng audit (script constant, ghi rationale từng dòng) → WARN (non-finding).
- **A8 — RBAC authoritative list** := static scan `hasRole("ADMIN")`/`hasAuthority` trong SecurityConfigs + `@PreAuthorize` của admin controllers, freeze vào report (superset của epic §2.8 + context pack — gồm newsletter, audit/log events, users, orders, reviews, loyalty, rma, coupons, categories, products, stocks, affiliates).
- **A9 — YAML parse:** hand-rolled line-level stdlib-only (không yaml lib — dep freeze; precedent scripts/qa/*.mjs đều node-stdlib). Parser fail-loud → exit 2 khi gặp shape không parse được (không đoán mò).
- **A10 — `--self-test`:** exit 0 khi fixture cho đúng expected (DANGEROUS bắt được), 1 khi fail. Fixture phủ cả 3 trục (files trong `scripts/qa/fixtures/`, KHÔNG tạm revert compose thật).
- **A11 — Finding IDs ổn định** (SF-4 merge vào bug-register): `CFG-A-xx` / `CFG-B-xx` / `CFG-C-xx` / `S2S-xx` / `RBAC-xx` / `CT-xx`.
- **A12 — Contracts freshness diff target:** `contracts/openapi/*.yaml` (paths+methods) vs `@*Mapping` controllers per service; stale → finding `CT-xx` riêng, không tự sửa.
