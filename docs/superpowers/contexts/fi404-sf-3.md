# SF-3 Context Pack — journey-specs

> Đọc file này THAY VỀ tự tổng hợp. Epic spec: `docs/superpowers/specs/2026-09-10-qa-sweep-design.md` · Bracket: `docs/superpowers/brackets/fi404-qa-sweep.md` · Linear: FI-404 (epic) · Nhánh đích: `story/fi404-qa-sweep`.
> **PRECONDITION:** SF-2 đã merge (harness chạy được — specs của bạn phải GREEN trên fresh-boot env do harness tạo). SF-1 đã merge (expected-matrix làm checklist). Pattern specs hiện có: `frontend/e2e/tests/*.spec.ts` + helpers/env.ts. Dep freeze.

## Spec slice (chỉ phần SF-3 chịu trách nhiệm)

1. **admin-journey.spec.ts**: login admin UI (`/login`, admin@demo.vn) → product CRUD ĐẦY ĐỦ FIELD round-trip: tạo product mới (name vi/en, desc, SEO title/desc, price, comparePrice, flashSale ends, tags = official checkbox, images, variants + stock) → save → **mở lại form assert từng field giữ nguyên giá trị** (chứng minh không còn field không-lưu — bug thật: stock) → edit → save → assert; coupon CRUD round-trip (code/type/value/minOrderValue/usageLimit/active/endsAt); category CRUD round-trip.
2. **data-lifecycle.spec.ts** (bắt lớp 3 + 4 + 5):
   - Lớp 3: guest (context mới) add product variant-less (seed: Sách Dooki / Nokia lúc đầu — sau seed fix mọi product có default variant → assert variant selector auto-chọn default) → login user@demo.vn (merge cart) → checkout pass qua bước validate (không `items[].variantId must not be null`); inject stale cart (API tạo cart row variantId null qua redis-cli hoặc mock) → assert repair-path hoặc lỗi rõ ràng (theo behavior fix: CartService.java:127 guard + seed default variant — cite fix commit trong spec comment).
   - Lớp 4 asserts surfacing: admin form mở product edit → stock input = inventory quantity thật (fetch availability); PDP không render "Đã bán" từ ratingCount (assert text); giá variant hiển thị = base + priceDelta.
   - Lớp 5 fresh-state: SW cache version thay đổi sau rebuild (nếu có SW — assert version key khác); bundle hash của entry script khác build cũ; port-owner check đã ở harness stage-up (SF-2) — spec chỉ assert page sống sau fresh boot.
3. **Helpers**: re-login helper theo section (JWT 15' — không giữ storageState xuyên 15 phút; pattern auth-cookie.spec); data-testid theo ADR SF-1 FI-390 (semantic classname giữ; thêm testid khi selector mơ hồ).
4. **Chạy trên fresh-boot env**: specs chạy SAU harness (env :8080 fresh + seeded) — local run qua `make qa-fresh-boot` rồi `pnpm e2e` specs mới; KHÔNG tự down -v (harness là chủ).
5. **Asserts số thật** (lớp 4): seed biết trước — 24 products, Nokia 590.000₫, Mustela variant delta 1000 → 481.000₫, stock 100 — assert số trên UI/API khớp (không placeholder).

## Touch map (files SF-3 tạo/sở hữu)

```
frontend/e2e/tests/admin-journey.spec.ts     (sở hữu — mới)
frontend/e2e/tests/data-lifecycle.spec.ts    (sở hữu — mới)
frontend/e2e/helpers/*                       (đụng nếu cần helper re-login chung — không sửa helper hiện có)
docs/superpowers/qa/report-sf3.md            (sở hữu — kết quả run fresh-boot)
```
READ-ONLY: specs cũ 15 file (KHÔNG sửa — nếu fix finding làm đỏ expectation cũ → ghi bug-register, escalate), apps/packages code, scripts/qa harness (SF-2), contracts. Playwright config hiện tại: serial, workers=1 — GIỮ.

## ACCEPTANCE (user-visible)

1. admin-journey: tạo product đầy đủ field + variant stock → save → mở lại → MỌI field giữ đúng (bao gồm stock = số đã nhập); coupon + category CRUD round-trip pass.
2. data-lifecycle: variant-less product add-to-cart → login merge → checkout KHÔNG văng "variantId must not be null"; stale cart row được xử lý rõ ràng.
3. Surfacing: admin form stock hiển thị số inventory thật (không 0 ảo); PDP không có "Đã bán" fake; giá = base + delta.
4. Cả 2 spec XANH chạy ngay sau `make qa-fresh-boot` (fresh env) — evidence log + video/screenshot.
5. Spec cũ 15 file không bị sửa (git diff evidence).

## Boundary (KHÔNG làm)

- KHÔNG tự chạy `down -v`/fresh-boot (gọi harness — SF-2 own); KHÔNG sửa harness (SF-2); KHÔNG sửa config-audit scripts (SF-1).
- KHÔNG sửa specs cũ (nếu expectation cũ sai theo fix → ghi bug-register escalate); KHÔNG sửa app code tìm thấy bug (→ bug-register + fix task SF-4); KHÔNG thêm dependency.
- KHÔNG run full sweep/fix (SF-4); KHÔNG merge vào main.
