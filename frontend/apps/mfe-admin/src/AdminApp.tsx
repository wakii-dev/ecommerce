import type { ReactElement } from 'react';

// Placeholder Task 1 — guard + layout + route table thay ở Task 2.
export default function AdminApp(): ReactElement {
  return (
    <div className="admin-root">
      <h1>Quản trị</h1>
    </div>
  );
}
