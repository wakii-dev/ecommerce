// Chỗ ở cũ — giữ path sống cho App.tsx/RemotePage/skeleton/mfe-admin test
// không nằm trong touch map SF-1; nguồn thật = @ecommerce/chrome (FI-398).
export { HeaderSlots, HEADER_SLOTS_CHANGED_EVENT } from '@ecommerce/chrome';
export type { SlotKey } from '@ecommerce/chrome';
