/**
 * Stub Task 10 — trang thật (hero carousel + flash deal + featured grid)
 * landed Task 11. Header/Footer + locale routing + metadata đã sống ở layout.
 */
export default function HomePage() {
  return (
    <div className="container home-stub">
      <h1>Shop VN</h1>
      <p>Home — Task 11</p>
    </div>
  );
}
